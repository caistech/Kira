Kira Coding Environment

OS: Windows
Shell: PowerShell
IDE: PyCharm
Project root: C:\Users\denni\PycharmProjects\Kira

Primary tools:
- OpenCode
- OmniRoute
- Git
- GitHub CLI
- Vercel CLI
- Supabase CLI

Rules:
1. Always assume PowerShell syntax.
2. Never use Bash operators such as ||, && or $(...) unless explicitly invoking bash.
3. Never invent CLI flags.
4. Run --help when uncertain about CLI syntax.
5. Read before modifying.
6. Never deploy without explicit instruction.
7. Never push without explicit instruction.
8. Never run destructive database commands without explicit instruction.
9. Before modifying code, inspect relevant files and understand existing architecture.
10. After modifications, run appropriate tests/build/type checks.
11. Report exactly what was changed.
12. For Git operations, state branch and working-tree status before making changes.