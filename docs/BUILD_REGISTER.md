# Build register — Kira

> ## M. 2026-08-06 — a real user found two bugs, and the gates that should have found them first
>
> **Shah Hussain (founder of Mnemo, evaluating for a deep-recall integration) reported two things.**
> Both were real, both were ours, and they were compounding each other. Shipped in **PR #64**;
> production verified serving `8923bd6` by `portfolio-gate-deploy-status` before anything was claimed.
>
> | | |
> |---|---|
> | **The session** — "the login is not persisting across my visiting tabs" | `middleware` refreshed the expired access token, Supabase **rotated** the refresh token and retired the old one after 10s, and every guard then returned `NextResponse.redirect(...)` — a different object — so the replacement never reached his browser. Minted, consumed, dropped. `redirectPreservingSession()` copies the cookies across. ⚠️ **His proposed fix would not have worked:** `sessions_timebox` and `sessions_inactivity_timeout` are both `0`, so sessions already never expire. A 30-day token addresses an expiry that was never happening. |
> | **The setup screen** — "it redirects me to setting up business" | Not a loop. A **door with no key**: `UserShell` redirected on `canSend(identity)`, which requires an **11-digit ABN** and an Australian state, so no owner outside Australia could ever satisfy it. `app/dashboard` carried a second copy, so fixing one would have changed nothing. Now gates **sending** rather than the product, with a dashboard prompt that names the ABN out loud. `canSend` itself untouched — no compliance check weakened. |
> | **Why K22 did not already cover it** | The Canada work is **recipient**-side (`checkCommercialJurisdiction` refuses commercial mail *to* a non-AU inbox). This is who Kira writes **as**. There is even a `country` on the identity record; `canSend` ignores it. Two different questions that read alike. |
>
> **Three verification gaps, each worse than the bug it hid:**
>
> | | |
> |---|---|
> | **The suite tested a predicate and never its consequence** | Six `canSend` cases, all asking "does it return the right answer?" It always did — including for Shah, where `false` was **correct**. Nothing asked what the product *does* with a false. Now pinned in the `canSend` block, with the fixture this file never had: an owner with no ABN. |
> | **No fixture or tester could ever hit it** | Both QA identities and the operator's account are complete Australian businesses, so every run sails past the branch that traps a new or foreign owner. `app/setup/layout.tsx` said the same of the thirteen-hop loop: *"it survived because nobody had walked it."* |
> | **ESLint had not started in months** | `react-hooks/set-state-in-effect` sat in a config object with no `plugins` key → exit 2 before reading a file. So `npm run lint` reported **nothing about the code** and the gate's Lint step failed on every push. Fixing it **unmasked** a second dead step (design tokens) that had never run, because the job dies at the first failure. **A gate that cannot start looks exactly like a gate that found something.** |
>
> **Also corrected today:** section **A** was stale in a way that steered a session wrong — see its own banner. **K23/K24** settled and closed. My own claim that production ran `eef18e9` was wrong three times; it was on `e935400` and I had never pulled.

> ## L. 2026-08-05 — what closed, and what this day changed
>
> **⚠️ SEVERAL ROWS IN THIS FILE WERE STALE AND STEERED AWAY FROM THE CORRECT ACTION.** K1 and K2 were
> marked open after being fixed; the LLD warned against a valuation re-weighting that had been done
> two days earlier; the nine-area row said unbuilt when `areas.ts` had shipped it. Found by re-reading
> the register against `git log`, not by anyone noticing. **Check the code before trusting a row here.**
>
> **The reported problem** — "she is drifting, no recollection of the Lot 442 conversation, talking
> over me" — resolved into three causes, none of them the model. Full account: `docs/DRIFT_AND_GUARDS.md`.
>
> | Closed | |
> |---|---|
> | Recall showed **10 of 91** memories, importance-ordered; 71 unreachable | union of importance + recency lanes, `none` excluded. Live, verified on the real agent |
> | Prompt asserted a **January signup snapshot** as fact ("diesel injectors") | removed from the prompt entirely — a caveat had already been tried and failed |
> | `recall_memory` had **no observable trigger** | rewritten; the description is the enforcement point, not the prompt |
> | Prompt named **three tools that do not exist** | tool list generated from the attached set; `tool-manifest.mjs` is the one list |
> | Persona upgrade **silently no-op for six months** | heading-anchored span replace; `unreachable` now counts as a failure |
> | **K1** handover carried private notes · **K2** fake ABN accepted | both were already fixed; rows were stale |
> | **A1–A4** valuation overclaim | fixed 08-03/04. Landing carried pre-correction figures for two days — gap overstated **2.25×** — now pinned to the calculator by a test |
> | **No write path** — everything terminated in our database | renderer → `file_manual` → orchestrator → Drive. **Verified with ten real documents, updated in place on re-run, read back out** |
>
> | New / still open | |
> |---|---|
> | **B4 deactivation** | now **commercially load-bearing**: the step-down trigger needs its denominator |
> | **K3a** pre-`genome_about` rows | unreachable by the guard; needs the reviewed re-classification pass |
> | **K19** checksum-valid but fake ABN | narrower than first written — the manual box is a genuine fallback |
> | **Owner-copy filing** | only the buyer copy has ever been filed to Drive |
> | ~~**`sde-multiples.ts` is US data**~~ | ✅ **RESOLVED 2026-08-08 — and the premise was wrong.** AU sources publishing on an **SDE basis** corroborate the US medians: plumbing 2.62 vs a published AU 2.0–3.5, electrical 2.94 vs 2.5–4.0, restaurants 2.26 vs 1.5–2.5. The 1–1.5× quote is on a different basis (EBIT/net profit after a market salary) — the same SDE-vs-EBITDA confusion as A5, arriving from outside. The real defect was the **flat band**, now sector-scaled. See `lib/valuation/au-evidence.ts`. |
> | **`patch-agent-capabilities.mjs` must NOT be run** | it reorders sections and puts live prompts back out of step with source |
> | **Nothing is validated by a customer** | zero **customer** valuations — production holds **6 as at 2026-08-08, all internal** (verified read; see the K preamble); one active subscription, the operator's own |
>
> **Decisions of record now live in `docs/DECISIONS.md`** — the project framing, the ⅓ narrow maintain
> rate, the valuation band, no-vendor-lock, and the Drive scope default.

> **Every open item, in one place, with what blocks what.** Written 2026-08-02 because the same
> things kept being raised, agreed, skipped, and rediscovered a fortnight later — usually because
> they were recorded in a naive-tester report, a memory file, a code comment or a chat message, and
> no single surface held all of them at once.
>
> **The rule that keeps this honest:** an item leaves this file only when it is *done and observed*,
> not when it is *built*. Kira's recurring failure mode is a correct edit that is silently defeated
> downstream, so "shipped" and "working" are separate columns on purpose.
>
> **The rule that stops it becoming a graveyard** *(operator, 2026-08-02)*: **close items one at a
> time, and close them NOW unless something genuinely blocks them.** If an item surfaces mid-session
> and nothing depends on it, resolve it rather than adding a row — recording is a cost, not progress,
> and a queued item that keeps being deferred never closes. B12 was found while closing B1, filed,
> and then closed in the same session under this rule: twenty minutes, and it removed a live defect
> from a real conversation path. Only a genuine dependency (the `Blocked by` column) justifies
> queuing. **One theme per session** — that is what makes closing one at a time affordable.
>
> **Sources swept:** `OPEN_ITEMS.md`, `NEXT_SESSION.md`, `GENOME_BUYER_FORMAT.md` §8,
> `naive-tester-reports/2026-08-01-ray/ray.md`, the project memory files, a live audit of the
> ElevenLabs fleet, the prod database, and a real export pulled from the synthetic QA identity.

**Type** — `BUG` broken now · `MISS` needed and absent · `DEC` needs Dennis · `PROVE` built but never
observed working · `DEBT` works but overclaims or will bite.
**Sev** — `1` blocks a client or a demo · `2` blocks the next build · `3` real but survivable · `4` noted.

---

## A. The valuation model — the credibility problem

Raised 2026-08-02 by an external read of the valuation PDF, then confirmed against the code. The
model exceeds its own cited source, and the transferability score *is* the multiple rather than
sitting beside it.

> ⚠️ **THIS SECTION WAS STALE AND STEERED A SESSION WRONG ON 2026-08-06.** Every row below still
> read OPEN and cited `MODEL_VERSION = '2026-07-24.1'`; the model was rebuilt 08-03. **The live value
> is `2026-08-08.1`** (`lib/valuation/model.ts:129`, checked 2026-08-08) — this banner itself asserted
> `2026-08-04.1` and went stale on 08-08, which is the third recurrence of the class it was written
> about. **Do not cite a version from this file; read the constant.** On the original stale reading I
> told the operator A7 was the highest-value thing outstanding
> and that it needed his decision — **it had been decided and executed two days earlier.** Re-verified
> against the code and against production, row by row, and rewritten below. Same failure class as K1,
> K2 and the L-header warning; it recurred **within a day of that warning being written**, which is
> the argument for closing a row in the change that closes the item rather than in a later sweep.

| ID | Item | Type | Sev | Blocks | Detail |
|---|---|---|---|---|---|
| ~~**A1**~~ | ~~Multiples inflate above the cited dataset~~ | — | — | — | ✅ **CLOSED 2026-08-03 — band rebuilt.** `lib/valuation/model.ts:156–169` names A1–A4 individually as what was wrong and what replaced it. The old `ceiling = min(8, sector × 1.6 + sizePremium)` is gone; the sector figure is now treated as a **CENTRE** (the median of businesses that actually sold, at average readiness) rather than a floor, so the band no longer runs above its own printed source. |
| ~~**A2**~~ | ~~No micro-SME discount; the size adjustment only adds~~ | — | — | — | ✅ **CLOSED 2026-08-03** with A1 — same rebuild. |
| ~~**A3**~~ | ~~Size premium widens the ceiling only, never the floor~~ | — | — | — | ✅ **CLOSED 2026-08-03** with A1. Floor and ceiling are now separated quantities (`LLD.md` §6): *where you are* is the buyer's discount and the market's claim, *what Kira moves* is `SPREAD = 0.75` turns and ours. |
| ~~**A4**~~ | ~~Transferability score drives the valuation (§6 violation)~~ | — | — | — | ✅ **CLOSED 2026-08-03** with A1. The 3–6-turn spread that made the score *be* the valuation is gone; the claim is now **half a turn to a turn**, which is what this register said the defensible number was. |
| ~~**A5**~~ | ~~SDE vs EBITDA never disambiguated on screen~~ | — | — | — | ✅ **CLOSED** — `lib/valuation/sde-copy.ts` is one canonical definition consumed by the input label, the pre-entry reminder, the worked example and the confirmation note, and **both multiples are labelled `× SDE` on the cards** (`page.tsx:1036`, `:1042`). It also ships `NET_PROFIT_PHRASES_BANNED_IN_SDE_COPY` so a test fails if net-profit wording creeps back — a regression guard on a correctness bug, which is stronger than the fix. ⚠️ **Named residual:** the page says *which* basis it uses; it never says EBITDA multiples are higher for the same business. Whether that comparison is owed is a copy call, not a defect. |
| **A6** | Walk-away is the raw `tangibleAssets` input | DEBT | 4 ⬇ | — | ⚠️ **HALF CLOSED, and the half that closed is the one that mattered.** In the model `walkAway = round(Math.max(0, inputs.tangibleAssets \|\| 0))` — still book value, deliberately: the 40–60c realisable range is derived **at the page** (`page.tsx:1027–1032`) so no valuation already shown to anyone is re-priced and `MODEL_VERSION` stays untouched. On screen the headline **is** the realisable range with book value stated beneath, and a paragraph now explains why one figure carries a range and two do not (see **H5** — the sector table has one median and no dispersion, so any ± would be invented). So "an asset figure printed beside a going-concern figure with nothing reconciling them" no longer describes the page. ⚠️ **The `4 ⬇` grades the DEFECT, which shrank; the warning below grew and is a standing trap, not an open item** — if the two look contradictory, that is why. **The original warning is now MORE load-bearing, not less:** the 0.4/0.6 derivation lives in the page, so anyone "fixing the ratio" in the model breaks the reconciliation rather than improving it. |
| ~~**A7**~~ | ~~Re-weighting re-prices baselines already shown~~ | DEC | — | — | ✅ **DECIDED AND EXECUTED 2026-08-04 — rescore everyone**, not bump-and-freeze (`LLD.md:349`). `scripts/rescore-valuations.mjs` exists and ran: **all four production accounts carry a `2026-08-04.1` snapshot**, verified in the 08-06 export. Two also retain their `2026-08-03.1` snapshot, so the older scoring stays readable as what it was — which is the entire reason the version is stamped (`model.ts:118–128`). |
| **A8** | The gap block renders as washed-out overlapping grey in print/PDF | BUG | 2 | — | **STANDS — and the mechanism is now named rather than guessed.** The row previously said "unverified whether it is the print stylesheet or a capture artifact". It is neither a capture artifact nor a print stylesheet: **`app/business-valuation/page.tsx` has no `@media print` rule at all.** The gap block (`:1065`) is `grad-genome` — white text on a CSS `linear-gradient` **background** — and browsers drop background graphics when printing unless `print-color-adjust: exact` is declared, which it is not anywhere in the page. White text over a dropped background is exactly the reported symptom. `lib/genome/render.ts:193` proves the fix is understood elsewhere in this codebase; the valuation page never got it. ⚠️ The overlap half is not explained from source. ⚠️ Also noted in passing: that block is `#a78bfa/#8b5cf6/#f472b6` (`:467`) — **AI purple and pink, the `DESIGN.md` §7 anti-pattern J4 already removed from `/genome`**, still live on the page an owner takes to his accountant. That is a **K4** data point. |

⚠️ **A1–A4 WERE ALSO A PRICING PROBLEM, not only a credibility one** *(team meeting, 2026-08-02;
operator-confirmed)* — **which is why the 08-04 rescore was the right call and not merely the tidy
one.** The monthly rate is *"a dynamic based on a percentage of that gap"* (`priceForGap`, live,
with the **$499 + GST floor as the base**), so a model that inflates the gap **overcharges**, and a
broker who discounts the valuation is disputing the invoice rather than the scorecard. Freezing the
old snapshots would have left live quotes standing on a band we had just agreed overclaims.

✅ **The pricing model is NOT in conflict** — an earlier reading of this register said it was, and
that was wrong. The gap-derived range on a $499 base **is** the current model and stands; the
*"budget airline — low charge to get in, the add-ons are enormous"* framing describes the **add-on
tier above it** (voice humanisation, specialist agents), not a replacement base.

**A1–A4 were one decision, not four fixes, and it was taken as one** — rebuilt in a single change on
08-03. The claim is now what this register said was defensible: documentation is worth roughly half a
turn to a turn, showing up mostly as a discount *not taken* and a shorter DD. It does not turn a 1.5×
business into a 5× one — that needs a manager and recurring contracts, which is a different business
rather than a written-down one.

✅ **THE AU-MULTIPLES ITEM THAT SAT HERE IS CLOSED — 2026-08-08 — AND THE PREMISE WAS WRONG.**
This paragraph previously shouted that the outstanding work was an **input**: `sde-multiples.ts` is US
BizBuySell data against a Finn Group broker's **1–1.5× for Australian trade businesses**. AU sources
publishing on an **SDE basis** corroborate the US medians instead (plumbing 2.62 vs a published AU
2.0–3.5; electrical 2.94 vs 2.5–4.0), and the 1–1.5× quote is on a different basis — EBIT/net profit
after a market salary, the same SDE-vs-EBITDA confusion as **A5**, arriving from outside. The real
defect was the **flat band**, now sector-scaled (`lib/valuation/au-evidence.ts`). ⚠️ **It stayed
loudest-in-the-section for a day after the L-header recorded it resolved**, in the section whose own
banner is an apology for exactly that. Remaining in A: **A8** (a print stylesheet) and **A9** below.

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| **A9** | The AU-multiples closure is **done and reasoned, not done and observed** | PROVE | 2 | Raised by an external read 2026-08-08, and it is right against this file's own rule. The closure rests on an **inference made at a desk about a number a working broker said out loud** — that Simon's 1–1.5× was quoted on EBIT rather than SDE. Probably correct; never put to him. Meanwhile two sectors still sit outside their published AU bands as pinned residuals (hospitality ~3.05× vs 1.5–2.5; Bars/Pubs 2.86 outside range), so the contradiction is not fully dissolved by the basis argument alone. **Fix is one email: *"when you said 1–1.5, was that on SDE, or on profit after a market salary?"*** Costs nothing, converts a reasoned closure into an observed one, and is the same correction-seeking move **N1** is built on — asking the expert to correct the number is the play, and this is the cheapest instance of it available. |

---

## B. The Genome rubric — decided, almost entirely unbuilt

Shape decided in `GENOME_BUYER_FORMAT.md` (six ✅ decisions). Build items in dependency order.

| ID | Item | Type | Sev | Blocked by | Detail |
|---|---|---|---|---|---|
| ~~**B1**~~ | ~~`confirmed` count rendered nowhere~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`b47ff7c`)** — see *Closed* below. |
| ~~**B12**~~ | ~~She asks him to confirm facts the Genome throws away~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`fe200fe`)** — see *Closed* below. |
| **B2** | Nine-area model does not exist in code | MISS | 2 | — | `derive.ts` still ships the original six sections. **Gates B3–B6 and C1–C3.** ✅ **Shape now fully decided** — `GENOME_BUYER_FORMAT.md` §3.2 (2026-08-02): nine areas with the ten ranks as weighting, `only-you` re-filed with an `owner_dependent` bridge flag, `about: software` split into `assistant`/`systems`, the location ladder, and the whose-Genome test. What remains is the build plus a reviewed re-classification. |
| **B15** | Corrupted proper nouns propagate, and nothing checks them | BUG | **1** | — | **IRESS → "IRIS" ×3** and **F2K → "S2K" ×2** in the real Genome, both phonetic and both voice-transcription shaped. Provable from the data: one row spells *"Iress Open"* correctly, so the same entity is stored two ways and the wrong spelling outnumbers the right one 3:1. Proper nouns are the highest-value content here — clients, systems, lots, people — and once one memory is wrong, later distils reinforce it. The register-rewrite guard checks proper nouns *survive a rewrite*; nothing checks they were right **at capture**. A handover document naming two systems the business does not use is confidently wrong in front of an advisor. **Mitigation is B1's confirmation loop** — reading a fact back catches this immediately, which is a stronger argument for confirmation than the buyer-evidence one §2 makes. **Folded into the B2 conversation 2026-08-02** (`GENOME_BUYER_FORMAT.md` §3.2, with the argument cross-linked from §2): it is the same rows — the entity-spanning `none` row carrying *"IRIS and XPlan"* is exactly what the `systems` split promotes into Management & records, so **the re-classification publishes this defect rather than creating it** — and the four new areas are almost entirely proper nouns. ⚠️ **Not mitigated by the loop alone, and this is why it stays open after B1 closed:** the channel that corrupted the name is the channel checking it, so a **spoken** read-back of "IRIS" gets agreement and promotes the error to `confirmed`, the strongest label the document has. The catch requires the fact seen **spelled** — typed transport or `/my-genome`. Two undecided mechanisms: offer spell-sensitive facts in text, or flag an entity stored two ways before it is ever offered. |
| **B16** | Pre-guard CAS rows sit in the Factory2Key Genome | BUG | 2 | — | Three-plus rows about IRESS/XPlan — Corporate AI Solutions work, operator-confirmed — predate the `save_memory` entity guard and sit in the F2K Genome. `split-genome-entity.mjs` exists and the 52 already-parked memories are the CAS corpus, so the machinery is there; this is a reviewed pass. ⚠️ **One row spans BOTH entities** (*"…Lot 91, Lots 109 and 442, and integration with IRIS and XPlan…"*) — parking loses the F2K half, keeping it carries CAS content across. Splitting one fact in two is a rewrite of the owner's record and is an operator call. That same row is `none/?` and is the one most likely to surface into the Genome under B2's re-classification: corrupted name, wrong entity, and about to become visible. |
| **B17** | On a local-storage answer she should offer the migration, not record the gap | MISS | **2** ⬆ | B2 | §3.2 decided the behaviour: a human exec assistant gets records into the cloud rather than noting that she cannot reach them, which turns an empty area from a report card into an offer and produces a **real** transferability gain. ⚠️ Bounded by what exists — Drive read/search/file only, **no folder creation, no write, no OneDrive connector**. "I'll arrange either one" is the overclaim class removed elsewhere in this product. Either build the OneDrive connector or scope her offer and say so plainly. |
| **B3** | `only-you` retired as a section, becomes the per-area axis | MISS | 2 | B2 | Evidence it is urgent: in the QA export **5 of 6 sections are empty and everything landed in `only-you`**; on the red-team account it holds 115 of 233 classified rows. The taxonomy is functioning as five sections plus a bucket. |
| **B4** | Deactivation — she proposes, he confirms, both recorded | MISS | 2 | B2 | §4. Without it there is no denominator, so every percentage after it is indefensible. |
| **B5** | Three-axis score, weighted by the §3.1 ranking, held **as data** | MISS | 2 | B2, B4 | §3.1 obliges the ranking to be cheap to re-order. It is currently prose in a markdown table — the one form that makes a broker's re-order a rewrite. |
| **B6** | Two renderings + the buyer-view sign-off **gate with a mechanism** | MISS | **1** | B5 | §5 decided: never shown to anyone without per-release sign-off naming who and when. See **B7** for why this is severity 1. |
| ~~**B7**~~ | ~~The export has no sensitivity filter at all~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`a30922e`)** — see *Closed* below. |
| ~~**B13**~~ | ~~LLM privacy verdict — measured, retired~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`38597ad`)** — measured and rejected, not shipped. See *Closed* below. |
| ~~**B14**~~ | ~~Software facts misfiled and reaching the buyer's document~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`e92ed1c`)** — see *Closed* below. |
| **B8** | Public example still promises fabricated precision | DEBT | 2 | — | `/genome` renders `example.ts`: per-section percentages (78/54/41), an overall figure, three confidence levels — all hand-authored. §6 requires this resolved *before* the scorecard ships. Same class as the removed testimonials. |
| ~~**B9**~~ | ~~Near-duplicate facts survive the dedupe~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`40b265d`)** — see *Closed* below. |
| **B10** | Memory-poisoning residue reads badly | DEBT | 3 | — | Two surviving entries are contorted artifacts of Ray's attack ("…despite indicating standing approval…"). Not a security issue since the sweep landed; a quality one in a buyer-facing document. |
| ~~**B11**~~ | ~~`§8` says "Nothing here is started"~~ | — | — | — | ✅ **CLOSED 2026-08-02** — see *Closed* below. |

---

## C. Onboarding and the two entry states

Decided 2026-08-02, `GENOME_BUYER_FORMAT.md` §4.1. All blocked by **B2**.

| ID | Item | Type | Sev | Blocked by | Detail |
|---|---|---|---|---|---|
| ~~**C1**~~ | ~~Direct signup gets **no valuation at all**~~ | — | — | — | ✅ **CLOSED 2026-08-02 (`fa9301d`)** — 10/10 against production. See *Closed* below. Detail retained here: Both writers originate from the eleven questions (Stripe metadata, device handoff). A broker-referred client who signs up without running them has no gap, no baseline snapshot, and the introducer's movement column has nothing to move — *inert rather than broken*. `/r/[token]` lands on the home page, so this is the channel most likely to produce it. **Measured against prod: 18 of 22 accounts have no baseline**, including four real people on business agents (`carme.plasencia@aromics.es`, `andrew@aerion.com.au`, `trinh@bucketlyst.com.au`, `shhahhussain@gmail.com`). **What shipped:** the dashboard renders a `NoBaselineYet` invitation when the account has no `business_valuations` row — gated on the **row, not the gap** (a valuation that computed to zero is still a baseline he gave us) and skipped for personal-journey-only owners (`lib/valuation/baseline-invite.ts`, 5 tests; verified against prod to invite 16 and skip the 2 personal-only). The link carries `?from=app`, which turns the result page's `/plan` CTA into `/dashboard`, relabels it *"Back to Kira"* and **suppresses the price quote** — a paying owner was being re-quoted a monthly fee under a button selling him what he already has. Attachment then runs through the existing `ClaimStoredValuation` on `UserShell`. **An invitation, not a gate:** §4.1 decides the baseline eventually blocks as onboarding block 1, but that is **C3** and walling the dashboard today would trap every existing owner. **To observe:** sign up clean → see the card → run the eleven → confirm *"that's mine"* → a `business_valuations` row **and** a baseline snapshot exist. |
| **C2** | The eleven answers never seed the Genome | MISS | 2 | B2 | They sit in `business_valuations.inputs` as JSON; `derive.ts` reads only three scalars. Without this, onboarding re-asks what he answered minutes earlier. |
| **C3** | Onboarding question set — **4 blocks**, open/block/block/skip | MISS | 2 | B2, C2 | **Origin (first, no gate)** · Baseline (skipped if claimed) · Scope (always) · Depth (skippable). **Block 0 added 2026-08-02** — *why did you start this, what problem were you solving, what gap did you see* — decided to go **in front of everything**. It is not a warm-up: it is the only source in the model for **competition** and for **what we sell**, the two holes §3.2 names, and it surfaces founding owner-dependence (rank 2) unprompted, which is the one thing he would deny if asked directly. ⚠️ Must land as **facts not narrative**; must not be rendered as a form field; runs straight at **H3**'s do-not-infer-exit rule; and is the likeliest place to file the wrong entity's origin against the wrong Genome. |
| ~~**C6**~~ | ~~Two onboarding interviews built in parallel~~ | — | — | — | ✅ **NOT AN ISSUE — resolved by the operator 2026-08-02, same day it was raised.** Gareth's advisor-agent interview skill **folds into what we have**; it is not a second interrogation. §4.1's four blocks remain the question set. Kept as a closed row rather than deleted, because "two interviews" is an obvious-looking inference from the meeting notes and would otherwise be re-raised. |
| **C5** | `isUsableInputs` guards 4 of the 9 fields the model requires | BUG | 3 | — | Found while verifying C1. `ValuationInputs` has nine required fields; the claim route's guard checks `industry`, `annualProfit`, `ownerDependence`, `systems` and waves the rest through. A payload missing the other five passes validation, reaches `computeValuation`, produces non-finite numbers from score-map lookups on `undefined`, and dies at the insert as *"Could not save valuation"* — **the owner loses the baseline and is told nothing useful**, which is C1's own failure class re-entering through the front door. Not reachable from our current client, which sends all nine; the live risk is a **stale cached client** or the next field added to the model, at which point every in-flight payload starts failing the write while validation keeps saying yes. Fix is to validate against the model's actual shape, or have `computeValuation` refuse non-finite output rather than return it. |
| **C4** | Pre-fill must not count as confirmation | DEC | 3 | C3 | Constraint recorded in §4.1; needs enforcing when C3 is built, or day one opens high and never moves. |

---

## D. Agent fleet and behaviour

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| ~~**D1**~~ | ~~`Kira_Trinh_DevelopingThe_7f1c` is on 15 tools with no confirmation section~~ | — | — | ✅ **CLOSED 2026-08-02** — and **the item was wrong as written**. See *Closed* below. |
| ~~**D2**~~ | ~~Duplicate agent rows~~ | — | — | ✅ **CLOSED 2026-08-02** — real, but not where the item said. See *Closed* below. |
| **D3** | She often does not call `record_refusal` at all | BUG | 2 | Reproduced post-deploy; runtime logs show `check_tasks` and `search_drive` and not one `record_refusal`. Prose with no mechanism. The typed transport is where a mechanism is cheap — the tool-call decision is in our code. |
| **D4** | Speculation persists | BUG | 2 | *"it looks like everything is already done on that."* `taskLedgerSection` forbids that sentence **verbatim, as the anti-example**, and it still does not hold. |
| **D5** | 2 agent-name mismatches remain | BUG | 4 | Both synthetic identities. `first_name` on `dennis+qauser` and `dennis+redteam` is the email local part — and it prints into the export as *"Recorded by dennis+qauser"*. |

---

## E. Never proven live

Built, plausibly correct, never observed working. This section is the one that most often gets
mistaken for done.

| ID | Item | Sev | Who can prove it | Detail |
|---|---|---|---|---|
| ~~**E1**~~ | ~~Naive-tester round 5 → the share gate~~ | — | — | ✅ **CLOSED 2026-08-07 — the first PASS this product has ever recorded, and the share gate is OPEN.** `naive-tester-reports/2026-08-07-firstrun/ray.md`, **VERDICT: PASS**, against `main @ f246a42` on `https://kiraexec.com`, deployment confirmed serving *before* the run started. Ray was the gate, as this row required. ⚠️ **The PASS is bounded and the report says so at line 307:** *"The Stripe checkout, the password-after-payment step and whatever it leads to are completely"* untested — the persona will not pay. So a PASS on the funnel is **not** a PASS on the paid product, which is why **N5** gate 2 exists and why this closing does not retire it. |
| **E2** | One full doing-loop round trip | **1** | Dennis (voice) | dispatch → approve → send → callback → `done`. Long-standing; zero `done` rows. |
| **E3** | Drive + contact lookup by voice | 2 | Dennis (mic) | Both halves exist and scopes are granted. "Find my Lot 91 files" and "Roger at Quantum Surveys" are the two first tests. Headless has no mic. |
| **E4** | Valuation full path after the change | 2 | anyone | Run → close tab → sign up next day → see the baseline. Not walked since the confirm gate + 2-day TTL landed. |
| **E5** | Drift cron: authenticated invocation + real send | 2 | first 08:00 fire | Route deployed and fails closed (401). `CRON_SECRET` is `type=sensitive` so it cannot be probed from a dev machine — correct posture. `?dry=1` checks it without consuming a fingerprint. |
| **E6** | Red-team **inconclusive** branch | 3 | a real judge outage | Added 2026-08-02 (`cb4267d`). Schema verified live; drift logic covered by tests that fail without it; the branch itself has never executed. |
| **E7** | `genome-classify` straggler sweep | 3 | observation | Runs at :30. Write-time path is wired; the sweep has never been seen running. |
| ~~**E9**~~ | ~~**C1** — a clean signup reaching a baseline~~ | — | — | ✅ **OBSERVED 2026-08-02**, 10/10 against production with the QA baseline parked and restored. What remains is the **client-rendered result copy** under `?from=app` (heading / no price / "Back to Kira"), which needs a browser — folded into the next `/naive-tester` run rather than tracked separately, since a fresh-signup persona walks it anyway. |
| **E8** | Settings price positive path | 3 | first subscriber | `getSubscriptionPrice()` reads the real monthly from Stripe; no subscribed account exists, so only the null path has ever rendered. |
| **E10** | The 12-month cap actually stepping a subscription down | **2** | first subscription to reach period 13 | Added 2026-08-09 with **P6**. `stepDownIfCapReached` is unit-tested to 21 assertions and mutation-proven, and **has never run against Stripe.** It is now a published promise about money on four surfaces, so the first real firing is not a nice-to-have observation — it is the moment the claim becomes true or false. **Cheaper than waiting twelve months:** seed twelve `billing_periods_reported` rows against a Stripe *test-mode* subscription and open a thirteenth period. Assert three things, because two of them are where it would fail quietly: the item's price moved to a `kira-gst-maintain-*` lookup key, the amount is exactly `floor(band/3)`, and a **second** invocation returns `already_stepped_down` without touching Stripe. |

---

## F. Billing and go-live

✅ **DONE — KIRA IS LIVE AND TAKING REAL MONEY.** Both blockers below turned out to be already
satisfied when checked against the live Stripe account (2026-08-03) rather than trusted from this
file: live secret key, live webhook signing secret, a registered and enabled live endpoint, and a live
portal with cancellation disabled. **`STRIPE_LIVE_MODE` has since been flipped** — the K-section
header records `{"live":true}`, `sk_live_` in the live slot and real `cs_live_` sessions minted. This
section is closed; `lib/billing/copy.ts` makes every sentence on every surface follow that one flag.
⚠️ **The consequence outranks the rest of this file:** anything that misleads or exposes a paying
customer is now a live exposure, not a hypothetical one — which is why **K19** (a fake ABN reaching a
Spam Act footer) sits at P0 despite being an hour of work.

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| ~~**F1**~~ | ~~Live-mode Stripe webhook endpoint does not exist~~ | — | — | ✅ **ALREADY DONE — the entry was wrong.** Verified against the live Stripe account 2026-08-03: an **enabled** endpoint exists at `https://kira-rho.vercel.app/api/stripe/webhook` carrying `checkout.session.completed`, `customer.subscription.created/updated/deleted`. `STRIPE_WEBHOOK_SECRET_LIVE` is set in Vercel production+preview, and `stripeWebhookSecret()` refuses to fall back to the test secret when live. |
| ~~**F2**~~ | ~~`configure-billing-portal.mjs` never run against LIVE~~ | — | — | ✅ **ALREADY DONE — the entry was wrong.** Dry-run against LIVE 2026-08-03 reports configuration `bpc_1SrG1G…` with `subscription_cancel: false` — *"already disabled — nothing to do."* So the portal's cancel door is shut in live and the only cancel path is `/api/billing/cancel`, which passes `invoice_now: false` and honours the waiver. The reasoning in the original entry stands and is why the script exists; only the claim that it had never been run was untrue. |

---

## G. Blocking a real client (Factory2Key)

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| **G2** | Per-tenant `from` **and sender identity** resolution | DEBT | 3 ⬇ | **ORCHESTRATOR HALF DONE AND OBSERVED 2026-08-03** (`orchestrator@21bce58`): `tenants.from_email` added (migration `007`, applied to `xuzvurmprexhalnxgsdu`, column verified present), resolved per tenant in `drainEmailOutbox`, settable via `PUT /v1/tenants/:id/identity` (`fromEmail` on the wire contract). **F2K is set to `Factory2Key <noreply@updates.factory2key.com.au>`** with `reply_email` `dennis@factory2key.com.au`; every other tenant is deliberately `NULL` and falls back, with the fallback REPORTED per tenant (`DrainReport.usedFallbackFrom`) rather than silent. ⚠️ **What remains is narrower than this row originally said, and the reason matters.** The prior text named `lib/kira/swarm/stub.ts:281`/`:315` as the live defect. They are not live: `KIRA_SWARM_ADAPTER` **is** set in Vercel Production (the `STATE.md` line saying it is deliberately unset is stale), and the orchestrator DB shows real dispatches arriving — 12 tasks on the F2K tenant, 139 on red-team. So owner-behalf mail goes through the orchestrator path that is now fixed. The stub is the **fallback**, and `getSwarmCoordinator()` falls back to it on any unrecognised value — so a misspelt env var silently routes owner-behalf mail back through Kira's hardcoded CAS domain with Global Buildtech's ABN. That is a latent hole worth closing, not a live one. Kira's *own* product mail (magic link, welcome-back) correctly stays CAS; do not move it. |
| **G7** | Client sending-domain setup is not an onboarding step | MISS | 2 | **Decided 2026-08-03: it belongs in onboarding, collected by a FORM, never extracted from a voice conversation.** `DATA_STANDARD`'s decider — a DKIM key, a subdomain and an ABN are exact strings where being approximately right is total failure (D1 → STRUCTURED), so transcribing a 217-char base64 key by voice is a guaranteed defect. Form collects: the subdomain, **who controls their DNS** (self / registrar / IT provider — F2K's went via a Leapfrog Freshdesk ticket, a different workflow), entity + **ABN** + postal + phone + contact email (`senderFromEnv()` throws without name+email; the Spam Act footer is invalid without ABN and postal), the **sign-off name** Kira uses for them, and reply-to. System then creates the domain via the Resend API, emits the DNS rows as a **copy-paste block or file — never inline in prose** (F2K's values "kept getting mangled" in email), and polls to verified. **The portfolio-wide version of this — the form fields, the "form not talk" reasoning, and all five DNS traps — is written up canonically in `cais-shared-services/CLIENT_HANDOVER_KIT.md` §6.5** (not duplicated here on purpose; one source, per the anti-fork rule). Build alignable with that doc's self-serve key wizard. Blocks the 2nd tenant, not F2K. |
| **G3** | The one refused Genome rewrite | DEC | 3 | Dropping "Factory to Key" as the subject reads naturally *if* this Genome is F2K's; erases who if not. |
| **G4** | Three memories the entity classifier could not place | DEC | 3 | Two are Kira feature requests whose use case is F2K work. |
| **G5** | Four tasks awaiting approval | DEC | 2 | Approving one sends under **Factory2Key's ABN**. The AI-related ones were tests — discard rather than approve. |
| **G6** | Roger's email for the Lot 109 contour survey | DEC | 3 | Task `562ccf05` has `request.to = null` and the drain correctly refuses. `lookup_contact` may now resolve it. |

---

## H. UI, landing and product decisions

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| **H1** | SayFix pill overlays hero copy at 375px + the dashboard card | BUG | 2 | **Dennis owns the scope.** The engine avoids controls and is blind to content. ⚠️ Do **not** pass `position` as a local fix — it is handed to the engine as a weighted preference (+8) and that is what parked the widget on the Back button for an entire valuation. |
| **H2** | AI-generated face on the landing page | DEC | 3 | Dennis: leave for now. Objection is consistency — a fake person illustrating a *don't fake it* argument. |
| **H3** | Ray's Opportunity items | DEC | 3 | 12th question · selling the refusal on the landing page · example Genome beside the hero · private mode · broker paragraph. ⚠️ On the 12th question: **do not assume exit intent.** If retirement or sale comes up it is a learning; inferring it from a valuation run is not. |
| **H4** | Over-promise attack: 5/5 held, no fix written | — | 4 | Either Ray hit a flake or the attack does not reproduce his case (his trigger was a document *name*; ours is a generic Drive ask). Writing a fix now would guard nothing. |
| **H5** | Range asymmetry — resolved by explanation, deliberately | — | 4 | The sector table has one median and no within-sector dispersion, so any ± would be invented. **If someone "fixes" this later by adding a band, they are fabricating.** |

---

## I. Residuals and known traps

| ID | Item | Sev | Detail |
|---|---|---|---|
| **I1** | Register rewrite can shift meaning the guard cannot see | 3 | It checks figures and proper nouns survive; it cannot catch *"Dennis is based in Perth"* → *"The business is based in Perth"*. Originals in `kira_memory.content_original`; `--revert --apply` restores. |
| **I2** | `billing.integration.test.ts` flakes | 3 | Stateful against live Stripe test mode. Failed once on the out-of-order assertion, then passed in isolation and on three full runs. **If CI goes red there, suspect this before anything else.** |
| **I3** | Four memories still name the owner | 4 | All `none`-classified, so filtered out of the Genome and never displayed. |
| ~~**I5**~~ | **STALE — Public voice widget dies for ~1 in 5 visitors — `@caistech/elevenlabs-convai`, not Kira** | — | ✅ **RESOLVED — the fix shipped in the package and Kira consumes it.** This row and **K14** contradicted each other (found by an external read 2026-08-08): I5 said the fallback is still gated on `status === 'error'` with no timeout, K14 said other products carry that *"until each bumps"*. **K14 is the correct one.** `@caistech/elevenlabs-convai` **0.12.0** added the stall timeout (`DEFAULT_FALLBACK_AFTER_MS` 8s, `shouldUseTextFallback(props, status, {stalled})`), and Kira runs **`^0.12.2`** (`package.json:20`). The measurement below was taken on prod `5a7562b`, **before** that bump. **K9 is the live evidence the fix is working** — *"voice text box takes 8–13s when the connection stalls"* is precisely the 8s timer firing where the box previously never appeared. **K9 is now the residual and owns this**: 8 seconds is deliberate (firing early would steal a slow-but-working connection from someone who does have a mic), but an *"or type instead"* offered up front beats any timer. Original: Click "Talk to the assistant" with no working mic and the panel opens; the text box then appears in **~570ms** or **never**. Measured on prod `5a7562b` across six sweeps in one afternoon on unchanged code, the box appeared **0%, 40%, 50%, 70%, 80% and 100%** of the time; failing attempts stayed empty past 30s, showing a mic button the visitor cannot use. Cause is exact: `widget-logic.js` → `shouldUseTextFallback()` returns the fallback only when `status === 'error'`, and Kira supplies an `agentId` so `canConnect` is true. **The text box is gated on the voice connection FAILING first, and there is no timeout** — a WebRTC attempt that neither connects nor errors leaves the panel dead forever. Affects every consumer using `textFallback`, so the fix belongs in the package (fall back on a timeout, not only on `error`). Ray's original defect — box takes the question and says nothing — is **fixed**: 0 silent submissions in 14. Caught by `portfolio-gate-audit-input-response`; a single run passes ~1/3 of the time, which is why that check reports a rate. |
| **I4** | Second Kira account for Global Buildtech | 4 | Deferred by decision, not blocked. Architecture supports it — tenant = user id. The 52 parked memories are the corpus. ⚠️ **`tenant = user id` is now a known future constraint, not just a note** — §3.2 decided (2026-08-02) that **Kira survives the sale and changes bosses**, which requires the business-scoped half to move to a new owner account while the personal half stays with or is destroyed for the seller. That is a migration, not a config flag. `B7`'s sensitivity split is the mechanism that makes it possible; the privacy policy must describe succession **before** it happens (`REGULATORY_INCLUSIONS` I1 gap). |

---

---

## J. Round-5 reconciliation — verified 2026-08-03

The eight items left open by the concurrent session (`docs/HANDOFF_2026-08-03_CONCURRENT_SESSION.md` §4)
from `naive-tester-reports/2026-08-03-0526/ray.md`. **Checked against the code, one at a time.** Two
could not be settled from source and say so rather than guessing.

| # | Item | Verdict |
|---|---|---|
| ~~**J1**~~ | ~~Stripe checkout: no GST on the figure; "per unit / based on usage / Price varies"~~ | ✅ **CLOSED 2026-08-03, both halves, both observed.** **Half one — the GST suffix now renders** ("Kira Business Plan — Growth (+ GST)", twice), and the root cause was never copy: `ensureMeteredPrice` is idempotent on the lookup key so `productName`/`productDescription` apply only at Product creation, and Stripe **refuses to update a Product it auto-created** (confirmed by trying). The Growth product predated the suffix commit by ten days, so the fix was inert from the moment it shipped. Fixed by bumping `PRICE_LOOKUP_PREFIX` (`kira` → `kira-gst`), documented at the constant as the only mechanism by which checkout copy can change. **Half two — the fixed figure now sits beside the pay button:** *"$999 + GST each month. You are charged after the month has finished, never in advance — cancel before then and that month is on us."* Verified in the rendered DOM on `checkout.stripe.com`, host-asserted. Required `customText` on `@caistech/subscription-billing` (**v0.4.0**, published, catalog updated) — Kira is its only consumer, so propagation was one bump. ⚠️ **What remains is Stripe's and cannot be changed:** `Price varies` ×2, `A$0.00 due today`, `per unit`. All true — an arrears subscription is metered, so the amount genuinely is not known until the month closes. If a tester reads those as evasion again, the only remaining answer is owning the payment page with Payment Element, which is a project and carries PCI surface Kira does not have today. |
| **J2** | Free-beta vs arrears contradiction | ✅ **FIXED 2026-08-03** — and it was worse than filed. `lib/faq.ts` told introducers *"while we are in beta, payments are switched off entirely — nothing is being collected from anyone yet"*, which went **false the moment `STRIPE_LIVE_MODE` was flipped**, in the answer that sets their commission expectations. Removed. The same pass caught a second falsehood **I introduced**: the pricing answer still said the fee "is set to the size of that gap" after `f91e0cd` moved the band onto reported profit. Rewritten, and it now states the separation as the reassurance it is. |
| **J3** | "Recorded by Ray" vs emails signed "Pat Nolan" | ⚠️ **UNRESOLVED FROM SOURCE.** `export/route.ts:68` builds the name from `first_name`/`last_name` on the app user, so the mismatch is data, not logic — almost certainly the same root as **D5** (synthetic identities whose `first_name` is the email local part). Needs the account inspected; not closable by reading code. |
| ~~**J4**~~ | ~~Example Genome has 6 sections, the real one has 9~~ | ✅ **CLOSED 2026-08-03.** `people`, `assets` and `systems` added to `lib/genome/example.ts` in the same fictional plumbing business — the three omitted were the ones a due-diligence list opens with, on the ONE page a prospect sees before he pays. **It fell behind silently because `areas.test.ts` pinned `GENOME_AREAS` at nine and nothing pinned the example to `GENOME_AREAS`.** `example.test.ts` now compares the two directly — a sorted-key comparison, so a failure names the missing area instead of reporting `6 !== 9` — plus every area having entries and the gaps list being non-empty (an example where nothing is still in his head describes a business that does not need the product). **Mutation-proven:** removing `systems` fails naming `"systems"`; restoring it passes. ⚠️ **This GROWS B8 rather than resolving it** — three more hand-authored `coverage` figures now exist and `overallCoverage` averages nine instead of six, so the headline number moves. B8 still owns that decision. Fixed in passing: `/genome`'s `.grad-genome` and `.grad-coral` were `#a78bfa`/`#8b5cf6`/`#f472b6` — AI purple and pink, `DESIGN.md` §7's named anti-pattern, on the page just promoted into the hero as "See a real one". |
| ~~**J5**~~ | ~~Kira Exec claims it excludes test accounts, then lists two~~ | ✅ **CLOSED 2026-08-03.** Both halves fixed, because either alone leaves a lie on the screen. `isNonClientAccount()` now filters the cohort: the canonical QA identities (plus-tagged on OUR domains only) and everyone in `ADMIN_EMAILS`. **Deliberately conservative** — it does NOT pattern-match "test" or "qa" anywhere in an address, because a real owner at `test@bigplumbing.com.au` or a business called QA Plumbing must never silently vanish. Excluding a real client is the worse of the two errors: a stranger in the list is visible, a missing client is not, and 4 tests pin that direction specifically. The **sentence was also corrected** — it no longer claims to exclude personal Kiras by filtering, because it doesn't: they never enter, since the cohort is built from valuation / LOI / paid signals. Claiming the right outcome for the wrong reason is how this row was created. |
| **J6** | `/plan` has no sidebar; back link paints late | ⚠️ **PARTIALLY EXPLAINED.** `app/plan/layout.tsx` exists but mounts only `BackToAccount` — deliberately, per its own comment: `/plan` is a **public** page and the full shell would frame marketing as an app screen. So "no sidebar" is by design; "paints late" is the real half and is unverified. ⚠️ Separately, `BackToAccount` is still `amber-200/amber-50` — off-palette since `DESIGN.md`. |
| **J7** | Mic FAB overlays body text on My Genome | ⚠️ **NOT VERIFIED — needs an authenticated live pass.** Behind auth, so unreachable from source. Note the landing widget added in `625389d` is `placement="floating"` and is a **different mount**; this row is about My Genome's own FAB. |
| ~~**J8**~~ | ~~Raw export $1,094,292 vs $1,090,000 everywhere else~~ | ✅ **CLOSED 2026-08-03.** The JSON spread the derived genome verbatim while the markdown and every screen went through `formatMoneyApprox`, so the attachment contradicted the document to the dollar. `approxNumber()` is the numeric half of that same rule (3 significant figures) and is spread AFTER `...g`, which makes it an override rather than a competing second set of fields; each figure now also carries `…Displayed` — the exact string that was on screen — so the two can be checked without re-deriving anything. **Why round rather than keep the precision:** the figure comes from eleven multiple-choice answers, so digits past the third are arithmetic, not knowledge, and the product's most persuasive paragraph is the one explaining that. 4 tests pin `approxNumber` against `formatMoneyApprox` directly, so they cannot drift apart again. |

**Net: ZERO confirmed open — five fixed (J1, J2, J4, J5, J8), three unverifiable from source (J3, J6, J7) awaiting one live authenticated pass.**
J3 and J7 need a live authenticated pass; J6's remaining half needs a browser. None of them should be
marked done on a reading, which is the rule this section exists to honour.

---

## K. 2026-08-04 — the open list, numbered and prioritised

Written because the day's findings were reported one at a time as they were hit and never
consolidated, which is exactly the failure this register exists to prevent. Everything below is
either OPEN or was closed today; nothing is carried from chat without being stated here.

**Standing fact that sets the priority:** Stripe is **LIVE** (`{"live":true}`, `sk_live_` in the live
slot, real `cs_live_` sessions minted). Anything that misleads or exposes a paying customer outranks
everything else.

**Also established today, and it changes how the rest reads: NO CUSTOMER HAS EVER COME THROUGH THE
FUNNEL.** An earlier note in this session called `7f1c4e2f` "a real owner"; it is Dennis. Every claim
about what a customer would experience is inference from design, not observation.

✅ **THE COUNT IS SETTLED — production read directly 2026-08-08** (`kmrskyewwnwettlycpfe`, PostgREST,
service role, read-only): **6 valuations / 14 snapshots**, and the file's competing numbers were
**never a contradiction — they were three different dates on a growing table.**

| | | |
|---|---|---|
| K preamble, 08-04 | *"exactly TWO"* | wrong even then — corrected |
| **K23, 08-06** | 4 valuations / 6 snapshots | **correct at the time.** `brokertest` and `seedtest` did not exist yet |
| **Sequence item 7, 08-08** | *"all 6 stored valuations rescored"* | **correct, and now verified**: exactly 6 snapshots at `2026-08-08.1`, one per valuation, one per owner |

Arithmetic reconciles exactly: `2026-08-03.1` ×2 + `2026-08-04.1` ×6 + `2026-08-08.1` ×6 = 14.

**All six owners are internal** — `dennis@factory2key`, `mcmdennis@gmail`, `dennis+qauser`,
`dennis+invitetest`, `dennis+brokertest`, `dennis+seedtest`. The load-bearing claim above therefore
**stands unchanged: no customer has ever come through the funnel.** Gareth's row is genuinely absent,
consistent with **K23**/**K24**.

⚠️ **Two things worth carrying forward.** (1) **`business_valuations` has NO `model_version` column** —
the version lives only on `business_valuation_snapshots`, so a valuation row cannot tell you which
model produced it and the only way to ask is via its latest snapshot. (2) **A count in this file is
worthless without its date.** Three rows disagreed for a day, a session was sent to reconcile them,
and every one of them was right when written. **State counts as `N as at YYYY-MM-DD`.**

### P0 — would damage a paying customer

| # | Item | Type | State | Detail |
|---|---|---|---|---|
| ~~**K1**~~ | ~~Buyer handover carries Kira's PRIVATE notes~~ | — | ✅ **CLOSED 2026-08-04 (`f8edb7a`)** | New `how-he-works` private reason, anchored on the owner as subject plus an approval/permission/priority verb — never on "approval" alone, so "the Marlow job needs council approval" still travels. Placed last, which the tests caught: first draft claimed a sentence belonging to `exit-intent`. 102 genome tests. |
| ~~**K2**~~ | ~~Manual ABN field accepts a fake ABN~~ | — | ✅ **CLOSED 2026-08-03 (`3292712`)** — the row was stale when the K-list was written | `validateAbn` (the ABR's own weighted modulus, arithmetic — cannot fail open on an outage) now runs server-side in `app/setup/business/actions.ts:68`, so `99 999 999 999` is rejected before it can reach the handover or a Spam Act footer. ⚠️ **Residual, tracked as K19:** modulus-valid ≠ real. |
| **K19** | A checksum-valid but non-existent ABN still saves | MISS | **OPEN** | K2's residue. `AbnLookupField` **already reverse-resolves** — `components/AbnLookupField.tsx:77` sends `abn=<11 digits>` and the route returns the registered entity name — but the *manual* `abn` box in `BusinessIdentityForm` bypasses it entirely and is only checksum-checked. Two fields asking for one thing is also a real cost for a 66-year-old. Fix: the manual field runs the same reverse lookup on blur and shows the resolved entity back for confirmation; degrade to the modulus check alone when the ABR is unreachable. |
| **K3** | Kira's own meta-notes leak into the Genome | BUG | **HALF CLOSED 2026-08-04** | ⚠️ **The earlier diagnosis was wrong and the correction matters** — see K3a/K3b below. |
| ~~**K22**~~ | ~~AU-only jurisdiction guard not wired into the send path~~ | MISS | ✅ **CLOSED 2026-08-06** — orchestrator `66db700`. `checkCommercialJurisdiction` in `src/connectors/email.ts`, refuse-before-claim so the row stays pending and goes out unchanged if the clearance widens. **Scoped to COMMERCIAL sends, and that is the whole design:** the package guard throws on an *unknown* country as well as a disallowed one, and nothing tags contacts — so applying it to every send refuses an Australian tradesman emailing an Australian client at gmail, which is most of them. That is an outage wearing compliance as a costume. Transactional mail is recipient-initiated inside an existing relationship and exempt or consent-inferred in every regime concerned. Country from **ccTLD only** — no IP geolocation, name heuristics or provider guessing, each of which gives a confident answer that is sometimes wrong, and a wrong country here either blocks a legitimate send or waves through the one this exists to stop. 18 tests. **Residual:** a `.com` belonging to a genuinely foreign business is undetectable; a commercial send to one is *refused* (unknown blocks) rather than mis-sent, which is the safe direction. Original entry below. | `PRODUCT_STANDARDS` §9 requires `assertJurisdictionAllowed(recipientCountry)` in the send path: a non-AU recipient hard-throws until that country's consent/identification/unsubscribe rules are configured, and an **unknown** country blocks too. Grepped the orchestrator 2026-08-06 — `assertJurisdictionAllowed`, `recipientCountry` and `SUPPORTED_OUTREACH_JURISDICTIONS` appear **nowhere** in `src/` or `app/`. `@caistech/email-compliance` ≥0.2.0 ships it; nothing calls it. So Kira will currently send commercial email to any address in any country. **Why it is P0 rather than housekeeping: the exposure is the OWNER's, not ours.** He is the named sender — his entity, his ABN in the footer — so a send into CASL or GDPR/PECR territory is his breach, committed by a tool he bought to be safe. He has no way to know it happened. **Found sideways** while checking whether a Canadian tester could exercise the send path; the answer was yes, which is the bug. **Fix:** country on the contact record, `assertJurisdictionAllowed` in `src/connectors/email.ts` beside the existing compliance call, unknown-country blocks, and the refusal surfaces to the owner as a reason rather than a failure. ⚠️ LinkedIn is exempt (platform compliance) — scope is EMAIL only. Note this also gates the whole distributor story: a distributor with any offshore client cannot be onboarded until it exists. |

**K3, measured against production 2026-08-04** (717 active rows, read-only). The register previously
recorded this as "the `about=software → none` filter already hides 14/24 rows… the defect is the
handful that escape". That framing implied a leaky guard. **The guard leaks nothing:**

```
                         visible in Genome    about=assistant/software that leaked
dennis@factory2key              23                          0
dennis+redteam@                240                          0
dennis+qauser@                   2                          0
```

`derive.ts:782` enforces `about=assistant → section='none'` **in code**, and it has never once been
wrong. It simply never fires, because the classifier answered `business` or `systems`. **A guard
cannot catch a verdict that was never reached.** Two distinct defects were wearing one label:

| ID | Item | Type | State | Detail |
|---|---|---|---|---|
| **K3a** | Rows sectioned before `genome_about` existed are unreachable by the guard | DEBT | **OPEN** | `genome_about IS NULL` on **7 of 23** visible rows on the real Genome and **233 of 240** on the red-team account — sectioned by the pre-2026-08-02 classifier, which had no `about` concept. They can never be re-asked: `classifyPendingMemories` selects `.is('genome_section', null)`, so a row that already has a section is skipped forever, and `classifyForReview` — the path that would re-ask them — **writes nothing by design and has never been applied.** Mostly benign business facts, which is why this is DEBT and not the P0. Closing it is a reviewed run of the existing review path, and it is the same pass **B16** needs. |
| ~~**K3b**~~ | ~~The distiller writes Kira's own state in the business's voice~~ | — | ✅ **CLOSED 2026-08-04** | **This is what actually reached a buyer-facing area.** On the real Genome: *"There is an unresolved issue to verify access to the Gmail account to locate contacts like Chris Newton and Roger"* filed `business → **customers**`, and *"Google Contacts is the primary source for email addresses…; other lists are not accessible for this purpose"* filed `systems → systems`. Structural, not careless: the distiller reads a transcript in which Kira was *doing* the task, and her working state comes out in the third person — at which point "access to the Gmail account is unresolved" is grammatically identical to a fact about the business's records. The `systems` category added on 2026-08-02 to stop rank 10 being empty is *verbatim* the shape her connector notes take, so the split widened the aperture; and `derive.ts:790` then promotes `about=systems + section='none'` **into** the systems area — one-way, toward disclosure. **Fix:** the decision moved to the only place that still has the evidence. The distiller tags its own notes `assistant-state` (`lib/kira/memory-extract.ts`), and `classifyPendingMemories` files those `none`/`assistant` **without asking the model at all**. Filed, not deleted — `none` renders in the owner's "everything else you have told me" list, so he can still see it and remove it. The tag rides in `DistilledMemory.tags`, which already survives the canonical save into `kira_memory.tags`, so no `@caistech/elevenlabs-convai` change and no orphaned consumers. Both halves mutation-verified (reverting either turns the tests red); 10 new tests, 468 suite-wide. |

⚠️ **K3b is shipped, not yet observed.** It changes only *new* distils — the two rows named above are
already sectioned and are K3a's population, not this fix's. Observation is one voice or typed session
in which Kira hits a connector problem, followed by checking the resulting row is `none`.

### P1 — credibility with the buyer this product is written for

| # | Item | Type | State | Detail |
|---|---|---|---|---|
| **K4** | Four visual identities | DEBT | **OPEN** | Landing, valuation, checkout and login each look like a different product. Ray: "I can't tell if I'm still on the same website." Needs a design decision, not a fix. |
| **K5** | `/genome` paints slowly | BUG | **OPEN, unexplained** | It server-renders 3,935 characters, so the handoff's "move `'use client'` down" diagnosis was wrong. Cause unknown; invisible to an HTML fetch. |
| **K6** | Two competing scores | DEBT | **OPEN** | "Transferability 32/100" on the valuation vs "On the page 57%" in the Genome. Both are called "the number to watch". |
| **K7** | No debt field | MISS | **OPEN** | "Roughly what do you owe?" then "after debt, about $X to you." The only number the owner actually cares about. |
| **K8** | Nothing asks what the broker already said | MISS | **OPEN** | Every one of these owners has had that conversation. Engaging with it is the difference between being understood and being sold to. |
| **K9** | Voice text box takes 8–13s when the connection stalls | DEBT | **OPEN** | Enormously better than never (which is what it was), but an "or type instead" offered up front beats any timer. |
| **K20** | The paid agent could invent "I watch what you do" the same way the landing one did | DEBT | **OPEN, judged not worth a fleet re-provision yet** | The landing agent said it live, and nothing in any prompt contains the word — she generated it from "you are noticing how he actually operates". `prompts.ts` is safer ("capture as you go — save the business facts", "as you learn what they are working on"): conversation-framed, no observation language. But the same invention is available to it, and this is the claim this audience is least able to forgive, since they are being asked to connect email, Drive and accounts. Not fixed pre-emptively because it means re-provisioning ten live agents, which has twice caused a real regression here (tools stripped to 15/17, four prompt sections deleted). Fold the "never say you watch" line in at the NEXT reprovision that is happening anyway; assert it in `prompt-completeness.test.ts` at the same time. Evidence, if wanted first: check a paid transcript for observation verbs. |

### P2 — verification owed

| # | Item | Type | State | Detail |
|---|---|---|---|---|
| **K21** | Supabase custom SMTP silently REVERTED to the built-in mailer | BUG | **FIXED 2026-08-05, but the cause is unknown** | Two reset emails, same subject, different senders: **27 Jul from `noreply@updates.corporateaisolutions.com`** (Resend, correct) and **3 Aug from `noreply@mail.app.supabase.io`** (built-in mailer, 2/hr). So it was configured and then undone — this was never a missing setup. Nothing announces it: auth mail keeps flowing, from an address no recipient recognises, at a rate that dies under any real use, and the only symptom is a tester saying "I never got the email" — which reads as their spam filter. Re-applied via the Management API (`smtp.resend.com:465`, sender `Kira`, `rate_limit_email_sent: 30`). **Owed:** find what reverted it (a project pause/restore, a dashboard save, a config sync?) and add `smtp_host` to a periodic check — `PRODUCT_STANDARDS` §9 already requires the auth smoke-test to FAIL, not warn, when email is on the built-in mailer, and nothing enforces that here. Note the trap: `POST /auth/v1/recover` returns **200 regardless** (it deliberately hides whether the address exists), so it cannot be used to verify sending. The authoritative signal is the Resend send log.|

| # | Item | Type | State | Detail |
|---|---|---|---|---|
| **K10** | Signup never verified end to end | PROVE | **OPEN** | `recover` returns 200 and `otp` throttles (proving the mailer sends), but signup itself was not exercised because it creates a real account. One manual signup closes it. |
| ~~**K11**~~ | ~~Share gate still closed~~ | PROVE | ✅ **CLOSED 2026-08-07** | Superseded by **E1**: `naive-tester-reports/2026-08-07-firstrun/ray.md` records **VERDICT: PASS** against `main @ f246a42` on `https://kiraexec.com`. ⚠️ This row and E1 stated the same fact in two places and **both were still reading "never passed" on 08-08, a day after the PASS** — found by an external read of this file, not by anyone noticing. One fact, one row: E1 owns the share gate from here. |
| **K12** | Genome baseline never seen on mobile | PROVE | **OPEN** | Verified rendering at desktop on the QA account only; 375px unchecked. |
| **K25** | Shah's two fixes are shipped but not OBSERVED | PROVE | **OPEN** | Live on `8923bd6` and verified by deploy-status, tests and mutation-proven guards — none of which is a browser. The session fix in particular can only be confirmed by someone staying signed in across hours, and Shah is the only person positioned to do that. **He has been told it is fixed**, which makes this the kind of claim that costs credibility if wrong; a silence from him is NOT confirmation. Note the preview could not be walked either: `ssoProtection: all_except_custom_domains` and **no Protection-Bypass-for-Automation token is configured on the project** (`protectionBypass` empty), so no automated tester can reach a Kira preview at all. Provisioning one is an operator decision — it creates a standing bypass credential. |
| ~~**K23**~~ | ~~Is production actually missing two valuations?~~ | PROVE | ✅ **OBSERVED 2026-08-06** — and the standing position was wrong in both directions | Read directly from `kmrskyewwnwettlycpfe` (Kira's own Supabase — not the cockpit ref); full rows exported to `C:\Users\denni\Downloads\kira-valuations-prod-2026-08-06.json`. **Production holds 4 valuations / 6 snapshots, not 2 / 5** — but the count was never the question, because **none of the backup's four rows are the rows in production.** Every production row carries a different `id` and a later `created_at` (three re-created 08-03, one 08-06): `dennis@factory2key`, `dennis+qauser` (Ray), `mcmdennis@gmail` ("Tom"), and `dennis+invitetest@corporateaisolutions.com` — **the last is the operator working the invite path by hand in Supabase, confirmed, not a funnel entry.** Resolving the backup's two unidentified `user_id`s settles K16's premise rather than contradicting it: `7d3e7218` is **dennis+qaadmin** (synthetic, gap $183,447) and `00cfede3` is **gareth@plausible.gg** (real, gap **$4,264,500**, 07-31). **So Gareth's valuation is genuinely absent from production and the backup is its only copy.** ⚠️ **What is still open is a decision, not a fact — tracked as K24.** |
| ~~**K24**~~ | ~~Restore or discard the two backup-only valuations~~ | DEC | ✅ **DECIDED 2026-08-06 — DISCARD BOTH** | Operator: *"we deleted Gareth's because it was on the old model, he has been told."* So the production absence was **deliberate, not a loss** — which retires the whole restore question rather than answering it. `7d3e7218` (qaadmin) is synthetic. `00cfede3` is Gareth's, computed under `MODEL_VERSION = '2026-07-24.1'` — the weighting **A1–A4** say overclaims the gap — so restoring it would have made a figure we intend to re-weight the origin the introducer's movement column measures from (**A7**). He has been told (**K16**) and re-runs in three minutes against whatever the model becomes. **Consequence: nothing wanted remains in `C:\Users\denni\.kira-backups\valuations-backup-2026-08-03.json`, so it is now deletable** under `PRODUCT_STANDARDS` §9 — "delete once the restore verifies" reads, here, as *decided not to restore*. Deleting it is the last step and is deliberately left as an operator action, since it is real turnover and profit against real `user_id`s and there is no second copy. |

### P3 — portfolio / infrastructure

| # | Item | Type | State | Detail |
|---|---|---|---|---|
| **K13** | `@caistech/report-generator` emits CORRUPT PDFs on Linux | BUG | **OPEN** | `bad XRef entry`. Ruled out: version drift, a read-before-flush race, and the Node version (raising CI 20→24 made it WORSE). Cause is the platform. Whole suite skipped in CI, declared not deleted. Producing valid PDFs is that package's entire job. |
| **K14** | Other voice products still carry the old widget | DEBT | **OPEN** | Singify, BucketLyst, ExecutorAI keep the emoji launcher and the **error-only text fallback** until each bumps `@caistech/elevenlabs-convai`. **The stall-timeout fix is 0.12.0**; Kira is on `^0.12.2` and is done — this row is about the other consumers only. (It was contradicting **I5**, which was stale; see that row.) |
| **K15** | `single-statement` check not built | MISS | **OPEN** | The last of the six mechanised checks. Would have caught the pricing-block-vs-FAQ contradiction and ExecutorAI's three simultaneous commercial models. |
| **K26** | `portfolio-gate-deploy-status` fails locally for a reason that reads like a deploy fault | DEBT | **OPEN** | The token is on disk at `~/.vercel-token` and nothing exports it, so a local run prints `FAIL — missing VERCEL_TOKEN` and then `production does not match the expected ref`. The tool is behaving **correctly** (§0.5: it fails rather than skips, because a check quietly doing nothing is indistinguishable from one that passed) — the papercut is that the second line reads as a deployment problem when the cause is an unset variable, and gate zero is precisely the check someone runs while already worried. Fix is either exporting it in the shell profile or having the tool read `~/.vercel-token` the way `bug-memory.mjs` reads `~/.mnemo-token`. Cost of leaving it: someone reads a red gate-zero at 1am as "production is broken". |
| **K18** | `/start` is still the pre-Exec generic helper | DEBT | **OPEN** | "Meet Kira · Your friendly guide · Let's talk about work stuff · Review Framework", on the raw ElevenLabs CDN embed rather than the canonical widget. It is ALSO the provisioning flow (draft → /setup/draft/[id] → /api/kira/create → /chat/[agentId]), so it cannot simply be retired — every brand-new owner passes through it, and the operator walking the real funnel on 2026-08-04 hit it immediately after paying. Rebuilding it onto the Exec shape is the fix; pointing the dashboard at `talkHref` (done) only helps owners who already have an agent. |

### Operator only — nobody else can do these

| # | Item | Detail |
|---|---|---|
| ~~**K16**~~ | ✅ **CLOSED 2026-08-06** — Dennis has told Gareth. He now knows before looking, which was the whole of it. Original: *"Gareth has not been told his valuation was deleted. Real person, $4.26M gap, and the model has now changed underneath it as well. He can re-run in three minutes; he should not find out by looking."* |
| ~~**K17**~~ | ✅ **CLOSED 2026-08-06** — moved to `C:\Users\denni\.kira-backups\`, which is **outside every git repo**, with a `README.md` recording what it holds and the condition for deleting it. Verified after the move: both temp paths gone, file parses, record counts unchanged (4 valuations, 5 snapshots). Outside-a-repo rather than a gitignored directory *inside* one, because a `.gitignore` is one edit away from not applying and this is real turnover and profit against real `user_id`s. ⚠️ **This row's own closing text was STALE WITHIN THE SAME DAY and is corrected here.** It said the prod read *"was not run this session"* and that the backup was *"the only copy of whatever production is missing"* — **K23 ran that read on 08-06, nine rows below**, and **K24 decided DISCARD BOTH the same day**. So the restore question is retired rather than answered: the two backup-only rows are one synthetic (`dennis+qaadmin`) and Gareth's, deliberately deleted under the old model, and he has been told (**K16**). Nothing wanted remains in the file, which makes it **deletable** — left as an operator action because it holds real turnover and profit against real `user_id`s and there is no second copy. Original: *"still in a temp directory — `valuations-backup-2026-08-03.json`, containing real turnover and profit. Third day. Temp directories get cleaned."* |

### Closed today

Auth 500s on signup/reset/magic-link (the Resend key in Supabase had diverged) · blank `/plan`
`/login` `/signup` · the voice agent being a chatbot (wrong transport — now canonical signed-URL
WebSocket, in the page flow, with the avatar) · the microphone emoji · doubled `+ GST + GST` ·
15px secondary text · the dashboard's three-different-numbers gap · the valuation starting at 4–6x
for a business entirely in the owner's head · nine empty Genome areas · **K1** the handover carrying
how he wants to work with Kira · the `/setup/business` self-redirect that trapped every brand-new
owner for thirteen hops · **K3b** the distiller writing Kira's own state as a fact about the business.

**Two rows in this list were stale when it was written** (K1 landed hours later; **K2 had been fixed
the previous day**). Both were found by re-reading the register against `git log`, not by anyone
noticing — which is the argument for closing a row in the same change that closes the item.

---

## N. Channel and go-to-market — the distributor read on the media plan

Raised 2026-08-08 from an external channel analysis of YouTube/podcast routes (Acquiring Minds,
Contrarian Thinking; The Site Shed, Lifestyle Tradie, UpScale, Profitable Tradie, TradesFormation,
Contractor Fight). Recorded here rather than in chat because it contains one decision that changes
who the customer is, and one gate that must sit in front of any public appearance.

**The analysis's own best line is the premise: the ICP is close to the least YouTube-native audience
in small business.** A 63-year-old owner who has not told his staff he is selling is not watching
business content. That is not an argument against these channels — it is the argument for treating
them as **distribution**, not as reach.

| ID | Item | Type | Sev | Blocked by | Detail |
|---|---|---|---|---|---|
| **N1** | Trades-coach hosts are **distributor prospects**, not media | DEC | 3 | — | A coach with 500+ trade-business owners through their programs is a distributor in the `BUSINESS_MODEL` §3 sense — existing book, can onsell, and owner-dependency is already in their curriculum. Same shape as Anneke and the brokers. **So the verb is the four gate questions, not "book an appearance"**; a podcast is the warm-up that gets the call taken. Their audience skewing 35–50 and growth-mode then stops being an objection, because we are not selling to their audience. Shortlist as named: Site Shed (Matt Jones, AU), Lifestyle Tradie, UpScale (AU/NZ), Profitable Tradie (NZ), TradesFormation (AU), Contractor Fight (US). **Needs Dennis:** which of these to approach, and whether the offer is the advisor 10% trailing (as with introducers) or a distributor clip. |
| **N2** | Buyer-funded transition capture — **post-LOI, never diligence** | DEC | 2 | — | The strongest commercial idea in the analysis and the one place its framing is wrong. **In diligence the incentives are opposed**: the seller wants to look less owner-dependent, the buyer wants owner-dependency proven to justify the key-man discount. A buyer-funded Kira at that moment is the instrument that documents the discount, working against the person doing the talking — and it would poison the seller-side proposition permanently. **Post-LOI or post-close the incentives align**: price agreed, buyer holds cash, extraction is the handover both sides now want. Same engine, different moment, different customer. Keep as a named second lane with a post-LOI trigger; **do not describe it publicly as a diligence product.** |
| **N3** | Whose Genome is a buyer-funded one — the consent surface | MISS | 2 | B6, I4 | N2 cannot be sold until this is answered, and it is architecture plus regulatory, not copy. Kira's entire trust posture rests on the owner disclosing things he has not told staff or family; a buyer paying the invoice changes who may read what, and today that is answered by code rather than by a surface anyone consents to. ⚠️ **B7 was listed as a blocker here and is CLOSED** (shipped 08-02) — corrected 08-08; it is the *mechanism*, not a dependency. So: **B7**'s sensitivity split already exists, **B6**'s per-release sign-off is the gate that is still owed, and **I4** already records that `tenant = user id` makes the succession case a migration and that *the privacy policy must describe succession before it happens* (`REGULATORY_INCLUSIONS` I1). N2 turns that from a future constraint into a precondition. |
| **N4** | Accountants, bookkeepers and industry associations are absent from the channel map | MISS | 3 | — | For an AU trade business the accountant is the most trusted advisor and usually the first person told about a sale — often before the family — and has a professional reason to raise succession unprompted. Same distributor shape, no content required. Associations (Master Plumbers, MBA, NECA) reach the 63-year-old through member newsletters and state branches — i.e. **the one channel that actually reaches the audience the analysis correctly says YouTube cannot.** Neither appears in the plan. Cheaper than guesting and does not consume operator hours the way an appearance does (Rule 1). |
| **N5** | **Gates before any public appearance** | PROVE | **1** | E-section | An appearance drives a warm audience to the URL, and the copy is **not** the weakest thing behind it. ⚠️ **Revised 2026-08-08 after an external read — the first four were the wrong four**, and E1/K11 have since closed (**the share gate is OPEN**, `f246a42`, VERDICT: PASS). **(1) The paid path walked end to end** — ✅ **DONE 2026-08-08, see section O.** It found **O1 (a paying owner gets no agent)** and **O6 (the onboarding route resets an existing account's password)**, both severity 1. The gate is therefore **not** satisfied by the walk — it is satisfied when O1 and O6 are closed. **(2) Signup→agent on a fresh account** — the identity form still appears to save nothing and agent creation is unproven (`project-kira-new-owner-path`; Shah was blocked by exactly this). **(3) B8 — `/genome` still renders hand-authored 78/54/41 percentages** on the page **J4 promoted into the hero as "See a real one"**: fabricated precision on the one page a prospect sees before paying, in front of an audience recruited by an argument about not faking it. This is the sharpest of the five and was missing from the original list. **(4) K6 — two competing scores** (*"Transferability 32/100"* vs *"On the page 57%"*), both called "the number to watch", in front of people who value businesses for a living. **(5) The valuation ties to its cited source on screen** — sector-scaled at `MODEL_VERSION 2026-08-08.1`, but the rescore is **not yet observed against real records and A9 is unasked**, so this is a PROVE, not a pass; the original text claimed "newly satisfied" and then undercut itself in the same sentence. Then copy. ⚠️ **K9** (voice text box 8–13s on a stalled connection) sits just behind these: the timeout fix is live, but a stalled first impression on the landing CTA is where a podcast audience arrives. |

⚠️ **The "ask the host to correct my numbers" move is right, with one condition.** It is the same
structure that worked with Simon Crisp, recorded — and it is the delegate-the-research half of the
operating loop. But it only reads as humility if the numbers are already defensible; being corrected
on trade multiples in front of an audience who sell trade businesses for a living is a credibility
burn otherwise. The AU-multiples work closed 08-08 and **reversed its own premise**, which is exactly
the kind of thing that should be settled *before* a recording rather than during one.

**Simon's objection — whether stubborn long-tenured owners engage with an automated assistant — is
now partly answerable rather than only askable.** The fresh-signup seed test ran 08-07 and passed:
first message, she named the trade, the 35 years, the two builders, and that he had not told staff or
family. One run is not evidence, but it is a better opening than a hypothesis.

⚠️ **Bandwidth.** Guesting is irreducibly operator time — the one part of this that cannot be handed
to the machine (Rule 1 / Rule 1B). Six distributor conversations outrank six appearances if forced to
choose; the appearance is a means to the conversation, not a parallel channel. Schedule it last.

---

## O. 2026-08-08 — THE PAID PATH, WALKED END TO END FOR THE FIRST TIME

**N5 gate 1 is closed.** Stripe TEST mode, local dev against production Supabase, real card form
completed by the operator by hand. Evidence: session `cs_test_b1DGN0qh…` reached **`status:
complete`** with subscription `sub_1U1yHGGocyoO80F7lqcm0XpS`; account
`dennis+paidtest@factory2key.com.au` (app id `7f2b5940`) exists and landed on `/dashboard?welcome=1`.

⚠️ **Independent evidence that this had genuinely never been done:** of the **20 checkout sessions in
Stripe's visible test-mode history, not one had ever reached `complete`** — every one was `open` or
`expired`. The register's claim was not merely unproven, it was measurably true.

**Confirmed working, several observed for the first time:**

| | |
|---|---|
| **J1's `customText`** | *"$999 + GST each month. You are charged after the month has finished, never in advance — cancel before then and that month is on us."* renders directly above Subscribe. Previously verified only by DOM assertion; now seen. |
| **Tax suffix** | "Kira Business Plan — Growth (+ GST)" renders twice on checkout. |
| **`val_first_name`** | The dashboard says **"Welcome, Ray"** — the name from the valuation intro, not the cardholder and not the email local part. The fix works end to end. |
| **J8's rounding** | Stored gap `230344`, screen reads **$232,000**. `formatMoneyApprox` holds across the boundary. |
| **Shah's ABN gate** | The dashboard shows *"Kira can't send email as you yet — everything else is ready"* and gates **sending**, not the product. Correct behaviour, observed. |
| **Chrome** | Overview / My Genome / Knowledge / Settings / Sign out all present on a brand-new paid account. |

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| **O1** | **A paying owner has NO Kira** — ⚠️ **SEAM FIXED 2026-08-08, the provisioning point deliberately NOT moved** | MISS | 2 ⬇ | `kira_agents` for the new paid account: **0 rows**. `kira_memory`: 0. **⚠️ THE DIAGNOSIS AND THE FIX WERE ALREADY WRITTEN DOWN** — `app/talk/page.tsx:70-80`, 6 August: *"a new owner HAS no agent, because agents are only ever created by POST /api/kira/create from the draft-approval flow, and `ensureUserAgent` — the canonical one-agent-per-user orchestration in `@caistech/elevenlabs-convai`, which exists exactly so this is not hand-rolled — is called nowhere in this repo."* Verified 08-08: `ensureUserAgent` appears in three docs and that one comment, and in **no application code**. What the walk adds is not the finding but the **evidence** — it now survives a real completed payment, not just a signup. **Why it keeps coming back:** every pass has fixed **routing** (which door the button opens) rather than the cause (there is nothing behind any door). `/talk` went `/start` → `/dashboard` (Ray's silent reload loop, 6 Aug) → the make-her flow; `dashboard:107` `talkHref` falls back to `/start` for the same reason. **`/start` cannot be retired while it is the only thing in the product that creates an agent** — which is the whole of **K18**, and the answer to "wasn't /start deprecated?" is *yes, and it is structurally undeletable until this closes.* **Fix: call `ensureUserAgent` on the paid path** (and/or on demand), per the `@caistech`-first rule. Noted in the original comment as deliberately out of that change's scope because it creates a real vendor resource on a page load. **✅ WHAT SHIPPED 2026-08-08 (operator chose "explicit setup step" over wiring `ensureUserAgent` at payment):** `app/onboarding/page.tsx` now sends a new paying owner to **`/start?journey=business&from=paid`** instead of `/dashboard?welcome=1`, so the paid path leads INTO the flow that makes her rather than onto a dashboard whose "Talk to Kira" button silently fell back there. `/start` gained a paid framing (*"Last step — let's set up your Kira · About three minutes…"*), **`autoConnect` was removed** so the package's *"Talk to the assistant"* launcher renders instead of dropping him into an open microphone, and "Back to selection" became "Back to your dashboard". The dashboard fallback is kept as the **recovery** route and named as such. **⚠️ `ensureUserAgent` is still called nowhere, and that is now a decision rather than an oversight** — two reasons, both recorded at the call site: an agent provisioned at payment has **no brief**, losing the seeded first message that made the 08-07 fresh-signup test pass (she named his trade, his 35 years, his two builders); and `create/route.ts:11` makes a **NEW agent per draft**, so provisioning early would hand him a second one the moment he finished the brief — the duplicate-agent state **D2** recorded. Closing O1 fully means collapsing create-per-draft into an update-the-existing-agent path first. **Verified:** `/start?journey=business&from=paid` renders the new copy, the launcher and the new back-link (live, 08-08); `tsc` clean; ESLint 0 errors. **NOT verified:** the post-payment redirect itself — that needs a second completed checkout, and this walk used the one session. |
| **O2** | `subscription_status` reads **`trial`** on an ARREARS subscription | BUG | 2 | `users.subscription_status = 'trial'` for `sub_1U1yHGGocyoO80F7lqcm0XpS`. Kira deliberately has **no trial** — `trialDays` was removed because arrears and a trial are different contracts that look identical for thirty days and then invert (`app/api/checkout/route.ts:84-89`). ⚠️ **Cause NOT established.** The webhook could not run (see **O4**), so this may be a column default that the live webhook would correct, or it may be wrong everywhere. Do not "fix" it before checking what the live webhook writes — and note `lib/billing/copy.ts` drives customer-facing sentences off billing state, so a wrong status here is a wrong sentence in front of a payer. |
| **O3** | **Two identical valuation snapshots** written by one onboarding | BUG | 2 | Both `model_version=2026-08-08.1`, `source=onboarding`, same reason string. The introducer movement column measures from these points, so a duplicated origin is two points at one timestamp on a curve that is supposed to show progress. ⚠️ **Cause NOT established** — React StrictMode double-invocation in `next dev` is a live candidate and would make this a dev-only artifact. Check a production onboarding before treating it as real. `recordValuationSnapshot` is explicitly never-throws, so a double write is silent by design. |
| **O4** | The webhook leg was **not exercised**, and cannot be locally | PROVE | 3 | `STRIPE_WEBHOOK_SECRET` and `STRIPE_WEBHOOK_SECRET_TEST` are both **unset**, so `stripeWebhookSecret()` throws in test mode. Everything above therefore proves the *redirect* path (`/onboarding` reads the session directly from Stripe) and says **nothing** about `checkout.session.completed` → subscription state. Closing it needs `stripe listen` supplying a secret, or the live pass. **O2 sits behind this.** |
| ~~**O5**~~ | ~~Three competing "talk to Kira" entry points~~ | — | — | ❌ **WITHDRAWN — I was wrong, corrected within the hour.** The three links are *"Start talking to Kira →"* (`talkHref`), *"Talk to Kira"* (the `TalkFab` → `/talk`) and *"Start discovery"* (`/discovery`). **`/talk` is a correct resolver**, not a stale link — it finds the owner's business Kira, deliberately prefers `journey_type='business'` over a more recent personal agent (an 08-05 incident where a personal Kira told a paying owner she had no Gmail/Drive/Xero, then distilled that denial into his memory), and renders `ChatPage` inline rather than redirecting so the address bar never shows `agent_7601kyxn…`. `/discovery` is a different surface, not a third door. What IS real is narrower: **two independent implementations of "find this owner's Kira"** — `talkHref` (`dashboard:106`) and `/talk` — which **have already diverged once, with the consequence above.** Worth collapsing onto one resolver; not the dead-end I filed. |
| ✅ **O9** | **`/start` → agent creation WALKED END TO END — it works** | PROVE | — | Closes the standing warning on `64092a6` (*"NOT VERIFIED END TO END. A real conversation through to agent creation has no test and I have not walked it"*) and the second half of **E2**'s neighbourhood. Walked 08-08 on the paid account via the **text-brief fallback** (automation has no mic): `/start?journey=business` → *"No microphone, or prefer to type?"* → name/location/brief → **Continue to review** → `/setup/draft/f9918912…` (email, name, location and the brief all carried through; **Business preselected**) → **Create My Kira** → `/chat/agent_9401kzfe0jskewsv23gy7m3qaeqp`. **The agent is real, not a silent tool-less one** (`project-kira-silent-toolless-agent`): `kira_agents` row `journey_type=business status=active`, name `Kira_Ray_RunPlumbing_7f2b`, llm `gpt-4.1-mini`, prompt **38,285 chars**, and **18 tools attached** — `get_conversation_context, save_message, update_conversation_topic, recall_memory, save_memory, search_knowledge, dispatch_task, approve_task, look_up_financials, check_tasks, search_drive, read_document, keep_document, lookup_contact, record_refusal, facts_to_confirm, confirm_fact, file_manual`. ⚠️ **The count is 18, and D1 recorded the fleet at 17** — `file_manual` (the Drive write path, section L) has landed since. If `verify-agent-fleet.mjs` still expects 17 it will now be wrong in the direction D1 warned about: a verifier whose expected set trails the fleet prints "consistent" over a real gap. Check before trusting it. |
| ~~**O10**~~ | ~~The draft page still offers **🌟 Personal** as a live choice~~ | — | — | ✅ **CLOSED 2026-08-08 — and it was worse than filed.** `page.tsx:56` initialised `journeyType` to **`'personal'`** and only corrected itself when the draft fetch returned, so a slow or failed fetch left a paying owner on the deprecated shape by default — not merely one mis-tap away. Three changes: state defaults to `'business'`; the draft's `journey_type` is **no longer read back** (a pre-deprecation draft still says `personal`, and honouring it would resurrect the shape); the toggle is **removed rather than disabled**, because a greyed-out option still tells an owner there is a version he is not getting. `journeyType` still travels in the create payload, so the API contract is unchanged. **Observed live** on a fresh draft (`6632bb4e`): no "Journey Type" label, and neither "Personal" nor "Business" appears anywhere on the review page. `tsc` clean, ESLint 0 errors. Original: | `/setup/draft/[draftId]` renders a Journey Type toggle with **Personal and Business**, on a product where `app/start/page.tsx:237-241` states *"Kira is business-only now — the personal journey is deprecated"* and forces every `?journey=` to business. Business is preselected, so the default is right and this is one mis-tap from wrong. **Why it is worse than "Back to selection":** a personal Kira carries the six memory tools and **none of the eighteen business ones** — no email, no Drive, no contacts. That is precisely the 08-05 incident recorded in `app/talk/page.tsx:20-28`, where a personal agent told a paying owner she had no connection to his Gmail, Drive or Xero **and then distilled that denial into his memory as a fact about his business.** A paying owner should not be able to select it at all. |
| **O11** | First message assumes continuity on a first-ever session | DEBT | 4 | The new agent's `first_message` is *"Hey Ray — good to hear from you. Let me see where we got to."* on an account with **zero `kira_memory` rows**. Defensible here, because the text brief genuinely did precede it — but it is the same shape as the "Hey Tom" stale-greeting incident, and it is the first sentence a paying owner hears. Worth deciding deliberately rather than inheriting. |
| **O8** | `/start` is coach-shaped, and **no shape was deleted** — three separate causes | DEBT | 2 | Operator, 08-08: *"not the correct shape — when we deduped the two Kiras we may have taken the wrong shape and deleted it."* **Investigated; the deletion hypothesis does not hold, and the real causes are more fixable.** (1) **"Kira, your coach" / "Voice assistant" comes from the PACKAGE, not from Kira.** `@caistech/elevenlabs-convai/dist/react/VoiceWidget.js:185,189` hardcodes `` `${coachName}, your coach` ``, `${coachName}, voice coach`, `coachName ?? 'Coach'` and the `'Voice assistant'` panel label. `/start` passes only `coachName="Kira"` (`app/start/page.tsx:379`). ⚠️ **There is no prop to override the coach vocabulary** — fixing it properly is a shared-package change, so the no-orphaned-consumers rule pulls in Singify, BucketLyst and ExecutorAI. That is a decision, not an edit. It arrived on **2026-08-04** with `64092a6`, which was itself correct (it replaced a CDN/WebRTC embed that was failing in production with a DataChannel error) — the migration imported the coach labels along with the working transport. (2) **"Back to selection" (`:284`) is a genuine dedupe leftover** — `:237-241` states *"Kira is business-only now — the personal journey is deprecated"* and resolves any `?journey=` to business, but the page still offers a back-link to a chooser with one option. Cheap, local, delete it. (3) **"Review Framework" / "Listening for your framework…" / "create a brief"** is pre-Exec product vocabulary the 08-04 migration deliberately left alone (*"The draft/brief handoff is untouched"*). Not lost — never rewritten. ⚠️ Also from that commit, still true: *"NOT VERIFIED END TO END. A real conversation through to agent creation has no test and I have not walked it"* — the same gap as **O1**. |
| **O7** | `/about` publicly offers **"Try Kira Free"** | BUG | 2 | `app/about/page.tsx:31` and `:145` — two CTAs reading *"Try Kira Free"* and *"Start Talking to Kira →"*, both pointing at `/start`, on a **public** page, for a product that charges **$999 + GST/month with no trial** (`trialDays` was deliberately removed — arrears is not a trial). Exactly the **J2** class: a free-beta promise that went false the moment `STRIPE_LIVE_MODE` was flipped, still live on a page a podcast or broker link would land on. This is what **K15**'s unbuilt `single-statement` check exists to catch, and it is now the second confirmed instance. |
| **O6** | `/api/onboarding/complete` **resets the password of any existing account** | BUG | **1** | Lines 63–75: when `createUser` fails because the email exists, it finds that user and calls `updateUserById(existing.id, { password })` with the password the caller just typed. Stripe never verifies that the email typed at checkout belongs to the payer. Mechanism: pay (arrears ⇒ **$0 due today**, attacker's own card), enter a victim's email, take `session_id` from the success URL, POST a chosen password → the victim's account, and their Genome, is yours. **Not executed — reported as a mechanism, not a confirmed exploit.** On a product whose entire proposition is that an owner tells it things he has not told his staff, this outranks everything else in this section. Secondary: `listUsers({perPage:200})` only searches the first 200 users. |

### O closures — the pre-run sweep, 2026-08-08 (production `f7e2f2d`)

Run before spending a `/naive-tester` pass, per TESTING_STANDARD §1: a run that reports defects
already written down is half wasted.

| | |
|---|---|
| **Mechanical sweep, prod** | `deploy-status` **PASS** (`f7e2f2d`) · `first-paint` **PASS** · `public-routes` **PASS** · `tax-suffix` **PASS** · `machine-routes` **PASS** · `memory-loop` **7/7 PASS** |
| ~~**O7**~~ | ✅ **CLOSED** — `/about`'s two "Try Kira Free" CTAs. It survived because `ad6e475` fixed `lib/faq.ts` and nothing else, while `c0a53ac` was *titled* "one billing model on every surface" and missed this page: TESTING_STANDARD §2.3, and the **second** confirmed miss `K15`'s unbuilt `single-statement` check would have caught. Now points at the valuation. |
| ~~**B8** + **K6**~~ | ✅ **CLOSED — and they were ONE defect.** `derive.ts:186` types real coverage as `'empty' \| 'thin' \| 'building' \| 'covered'` — **the product never emits a percentage**, so `/genome`'s 78/54/41 were not merely hand-authored, they were a different *data type* from the thing they illustrate. Meanwhile the real headline elsewhere is Transferability /100, so the page taught a second scale that contradicted the first. Now: per-area shows the product's four bands; the hero is Transferability /100; the figure is **derived** from the bands (`exampleTransferability`, example reads **59/100**). `lib/genome/timeline.ts` consumed the old export and was reconciled in the same change — no orphaned consumers. |
| **K9** | 🟡 **FIXED IN THE PACKAGE, NOT IN PRODUCTION.** The 8s is deliberate and stays (firing early *replaces* the voice UI and would steal a slow-but-working connection from someone who has a mic). The defect was the **silence**: with `transcript` set the status line is suppressed and the transcript only renders once connected, so the panel was empty for the whole wait — measured live, `.convai-status` empty at t=2/4/6s, box at t=8s. `@caistech/elevenlabs-convai` **0.13.0** adds a connecting state with a bounded bar tied to the same `fallbackAfterMs`; `shouldShowConnecting` is pure, 4 tests, mutation-verified. ⚠️ **Merged, NOT published** — Kira is still on `^0.12.2`, so this is not in front of a tester yet. |
| ~~**I5**~~ | ✅ **STAYS CLOSED — I reopened it wrongly and then re-closed it.** `audit-input-response` FAILed on `/` (1 of 5 answered), I called it real, then measured production directly: the box appears at **exactly t=8s**, the timeout working as designed. **The gate is what is wrong** — it looks before the deliberate delay elapses, so it will red *every* consumer using `textFallback`. Belongs back with `portfolio-gate`. Lesson recorded rather than hidden: the first closure reasoned from a version number, the reopen reasoned from a red check, and only the measurement settled it. |

⚠️ **What the walk did NOT cover.** The 11-question funnel UI (the session was created via `POST
/api/checkout`, because the browse daemon **crashed 3/3 on `checkout.stripe.com` headless** and cannot
survive a multi-step form; the 08-07 PASS covers that half). The live Stripe configuration. The
webhook. Anything after the dashboard's first screen — no conversation, no Genome, no `/start` walk.
Two card attempts failed first with *"Your card was declined"* because a **real** card was used, which
test mode always declines; that is Stripe protecting you, not a defect.

---

## P. 2026-08-09 — Ray's 08-08 walkthrough, triaged

`naive-tester-reports/2026-08-08-ray-prod/ray.md`, **VERDICT: FAIL** against production `f7e2f2d`.
Not on the product — *"the valuation engine, the question copy, the profit guard and the 'why the
number is what it is' essay are the best I've seen aimed at people like me, and the arithmetic
checks out to the dollar."* It failed on trust.

The two tab-closers were fixed the same day in `34a56dc` (the privacy contradiction, the footer
domain). **Everything else in the report was named in that commit message and filed nowhere**,
which is the failure this register exists to prevent — so it is filed here, in full, including the
items nobody intends to action soon.

⚠️ **One correction to how the report was carried forward.** The 08-08 commit listed "two `<header>`
elements on three pages" as one of eight leftovers. Checked as a class rather than as three
instances, it was **nine pages with two headers and six with two footers** — see **P1**. Ray sees
the pages he visits; a grep sees all of them, and only one of those two was ever going to find
`/privacy`.

### Closed 2026-08-09

| ID | Item | Type | What was OBSERVED |
|---|---|---|---|
| ~~**P1**~~ | Duplicate header/footer chrome, portfolio-wide | BUG 2 | ✅ **CLOSED — and it was three times the size of the report.** Counted `<header`/`<footer` in the HTML production actually served (`fd11f62`): **two headers** on `/genome` `/business-valuation` `/plan` `/commit` `/privacy` `/pubguard` `/terms`, plus `/introducer` and `/introducer/terms` behind auth where a 307 hides them from any anonymous count; **two footers** on `/about` `/privacy` `/pubguard` `/terms` `/advisors` `/what-she-does`. On the two LEGAL pages that meant the operator's identity rendered twice; on `/about` the two disagreed about the year — a hardcoded `© 2025` directly above `getFullYear()`'s `© 2026`, which the tester read exactly as it deserves: *"the kind of small thing I notice on an invoice."* **The cause was a single list doing two jobs.** `OWN_CHROME` governed header AND footer together, and its own comment defended that as a safety feature — *"same list, same test, so the two can never disagree."* They must: `/genome` and `/business-valuation` own their header and have **no footer of their own**, so one combined list could only choose which defect to ship — remove the duplicate header or remove the only footer, taking the operator's name off the page with it. Split into `OWN_HEADER` / `OWN_FOOTER`. **On `/privacy` and `/terms` the page's own footer is the one that survives**, decided by reading both rather than by preferring the shared component: those carry entity + ABN + trading-as + postal + contact (`REGULATORY_INCLUSIONS` identity) and `CorporateFooter` carries a lighter one with **no ABN**. `/about`'s own footer deleted outright — which also removed two links to `corporate-ai-solutions.vercel.app`, the same "the parent company doesn't have a website" class as the footer domain fixed in `34a56dc`, surviving that pass because it was looking at the certificate rather than at every link to the parent. `/plan`'s thin footer demoted to a `<div>` so the page that asks for money keeps the identity footer and stops shipping two. **The mechanism matters more than the fix:** the old comment predicted this exact failure and called it *"visible immediately."* It was not — it stood on nine routes until someone counted logos. `components/corporate/site-chrome.test.ts` now derives correct membership from the page files, so the requirement sits on the page a developer is editing rather than in a comment three directories away. **Mutation-proven the honest way — it failed first, naming six routes I had not fixed, and passes now (29 tests).** It also failed on my own comment explaining a removed `<footer>` by quoting the element; it strips comments, because `@caistech/portfolio-gate`'s offer-claims audit already learned that punishing an honest explanation teaches people to delete the explanation. ⚠️ **Stated limit:** it scans page files for a literal tag, so chrome arriving through a COMPONENT is invisible to it — `/pubguard` renders `<KiraFooter/>` and is in the list by measurement, not by the test. The complete version counts elements in the SERVED HTML and belongs in `portfolio-gate` beside the other live audits (**K15**'s neighbourhood). |
| ~~**P2**~~ | *"electrical contracting"* resolves to Residential Building | BUG **1** | ✅ **CLOSED.** A 66-year-old electrician of 35 years typed the most accurate description of himself available and was offered **one** option: Residential Building & General Contracting. Shortening it to *"electrical"* revealed the right sector — so **typing the more accurate phrase got the less accurate answer, and the correct category was invisible.** Severity 1 rather than 2 because the page states the consequence itself: the sector *"sets the multiple, so if it is wrong the number is too"*, and a builder's multiple is not a sparky's. This was a wrong **number** wearing a wrong label, on the product's best-built surface. **The cause is one line:** `synonymSector` ranked candidate keys by LENGTH, and `contracting` (11) is longer than `electrical` (10). Longest-wins is correct for the case it was written for — *"plumbing services"* must hit `plumbing`, not `plum` — because there both candidates describe the SAME trade at different lengths. It breaks the moment two candidates describe DIFFERENT trades, and then it silently prefers whichever has more letters. **Of the 191 keys in that table exactly ONE is a business-model word**, so the fix is a set of size one rather than a scoring model: a trade beats a model, and among equals the longer key still wins so every existing resolution is untouched. **Mutation-proven:** emptying the set turns exactly the 4 new tests red and leaves all 15 existing ones green, so longest-wins is provably intact. One test asserts through the **multiple** rather than the sector name, so the label and the arithmetic cannot drift apart again. |

### Open — filed, not fixed

| ID | Item | Type | Sev | Detail |
|---|---|---|---|---|
| **P3** | 8 of 9 example Genome areas *"are not clickable"* | BUG | 2 | ⚠️ **INVESTIGATED, AND THE REPORTED DIAGNOSIS IS WRONG — the handlers are real and they work.** Measured live on production, asserting `location.hostname` in the same call: 9 cards, 9 buttons, and clicking *"Who does the work"* after network idle expands it **112 → 817 characters** while card 1 collapses. What IS real: **a click issued immediately after the load event is silently dropped** — `aria-expanded` unchanged, no article's text length moves, and **no console errors**. So the page is served, readable, and inert for a window; `/genome` is `'use client'` end to end, so its entire interactivity waits on the bundle. ⚠️ **The gap is stated rather than closed:** the window measured only **~300ms** from a fast headless client on a fast link (goto 1045/1101/1234ms, then network idle +275/369/281ms), which does **not** explain a man clicking, waiting, and concluding it is not clickable. Either his client was far slower or something else is happening, and I have not established which. **This is almost certainly the same root as `K5`** (*"`/genome` paints slowly, cause unknown"*) — treat them as one item. **Candidate cause, found while looking and not yet measured:** six page files inline `@import url('https://fonts.googleapis.com/…')` inside a `<style>` element in the body — `about`, `business-valuation`, `genome`, `onboarding`, `plan`, `LandingClassic` — which is a render-blocking, late-discovered stylesheet on exactly the pages he called slow. `next/font` or a `<link>` in the root layout is the fix if it holds up. **Next step is a measurement, not an edit.** |
| **P4** | The sector's average multiple is referenced and never stated | MISS | **1** | *"which is why the multiple sits below your sector's average, not at it."* **Below by how much?** His words: *"If electrical contracting averages 4.1× and I'm at 2.8×, that's a fact I'd chew on for a week. As written it's a claim I can't check, on a page whose whole credibility rests on being checkable."* Severity 1 because the number is already in the codebase (`sde-multiples.ts`) — we are withholding the one figure that turns the argument from an assertion into arithmetic, on the page built to be checked. It also directly serves **N5** gate 5 (*the valuation ties to its cited source on screen*) and is far cheaper than anything else in that gate. |
| **P5** | The calculator's uplift is half the landing example's | DEBT | **1** | His result moved 2.8× → 3.2×, **15.5%**. The plumbing example on the landing goes $684k → $879k, **28.5%**. *"the marketing example is nearly twice as flattering as what the calculator gave me."* Worse in combination with the product's own language: he is told *"sell an asset, not a job"*, and the tool then puts him at 3.2× against a stated range topping at 5×, i.e. still selling a job after doing all the work. ⚠️ **This is the A1–A4 class arriving from the other end.** Those were fixed by claiming LESS; the landing example was not re-derived at the same time, so the marketing number now overstates what the corrected model produces. `landing-example.test.ts` exists — check whether it pins the example to the model or merely to itself. **Do not fix by re-inflating the model.** |
| ~~**P6**~~ | ~~`$999 + GST` a month, with no stated duration~~ | — | — | ✅ **CLOSED 2026-08-09 — a CAP, not the band he asked for.** Operator decision: **12 months at the full rate, then the maintain rate regardless**, recorded in `DECISIONS.md` §2 as trigger B alongside the still-undecided trigger A. **The distinction is the whole item.** He asked for an estimate (*"nine to fourteen months, call it $10k–$14k"*), and the product had been refusing on grounds that were correct — a forecast we cannot honour is worse than none, and six valuations, all internal, cannot support one. A **ceiling is the opposite shape**: a commitment we control, keepable unilaterally, needing no denominator, only elapsed billed months. So the promise the product could not make and the one it can look identical on the page and are opposites underneath. He also read our motive generously and was right — *"the refusal to estimate reads as evasion, when I think it's actually caution."* **Enforced, not merely stated** (`stepDownIfCapReached`, `lib/billing/arrears.ts`), because a published cap nothing implements is the **J2** failure with money attached. Counted from **our own `billing_periods_reported` ledger, not Stripe's `start_date`** — the promise is about months CHARGED, so a paused subscription must not step down on the calendar. Idempotent via a separate `MAINTAIN_LOOKUP_PREFIX`: without it a retried webhook takes a third **of a third**, and two retries put an owner on $37 instead of $333, silently and in his favour, which is the direction nobody audits. **Stated on four surfaces from one constant** (`FULL_RATE_PERIOD_CAP` in `pricing.ts`, so copy and enforcement cannot drift): landing pricing block, `lib/faq.ts`, the **result page** — where he said the sum actually gets done — and `/plan`'s terms line. ⚠️ **`maintain-rate.test.ts`'s blanket "no timeframe anywhere" assertion was REPLACED, not deleted.** It was right about trigger A, and deleting it would have dropped the only guard against *"typically nine to fourteen months"* appearing. It now pins the distinction: a ceiling is allowed, a forecast is not. **Mutation-proven twice** — injecting that exact forecast sentence turns it red, and removing the lookup-key check turns the third-of-a-third test red. **Observed** on the built landing: both cap statements render, the old *"we do not put a date on it"* is gone, no forecast wording. 21 tests in `arrears.test.ts`, 12 in `maintain-rate.test.ts`. ⚠️ **Not observed live:** the step-down itself has never fired — no subscription is near 12 periods, and the first one to reach it will be the first real run. |
| **P7** | Four questions a buyer asks and the eleven do not | MISS | 2 | **Debt** (~$380k of vehicle and equipment finance — currently a disclaimer at the bottom telling him to do the arithmetic himself), **the premises** (he owns the yard through his super fund; for a trade business often the biggest single question in the deal), **WIP and retentions** (real money on other people's balance sheets), and **when he wants out** (*"the entire product is aimed at a man in his sixties and never once asks how long he's got. Two years and eight years are completely different advice"*). His framing is the product one: one more screen turns *"an indicative valuation of the business"* into *"roughly what you'd walk away with"* — *"That's the number I'd screenshot and show my wife."* ⚠️ **Debt is already filed as `K7`** — merge rather than duplicate. ⚠️ *When he wants out* runs straight into **H3**'s do-not-infer-exit rule: asking is fine, inferring is not. |
| **P8** | The landing's three headline numbers are unexplained | MISS | 2 | `WALK AWAY $220k / TODAY $684k / CAPTURED $879k` with nothing saying what "walk away" means. *"Walk away from what? From the sale? From the business?"* He only found out deep inside the valuation. **The definition already exists word-perfect on the valuation intro page** — so this is a copy move, not a writing job, and it turns *"the most striking numbers on the page"* from mystery into the argument. Cheapest item in this section. |
| **P9** | The printout has no date, no business name, no inputs | MISS | 2 | *"If I print this and put it in a drawer, in six months I won't know what turnover figure it was based on or when I ran it."* A small block carrying date, sector and the four input figures *"turns it from a screenshot into a document"* — on the page explicitly built to be handed to an accountant. Pairs with **P10**. |
| **P10** | `SDE` used twice on the result page and expanded nowhere on it | DEBT | 3 | Defined on question 3, which he can no longer see. This is the page with **Save / print this** — the one that leaves the building. `lib/valuation/sde-copy.ts` is already the single canonical definition (**A5**), so this is consuming an existing constant, not writing new copy. |
| **P11** | *"Save / print this"* opens a print dialog | DEBT | 3 | *"'Save' in the label suggests a file, and what I got was a printer."* It then locked his page up for a couple of minutes and his browser died — he flags the lockup as possibly his machine, and does not claim it as a defect. The label is the certain half. |
| **P12** | `/plan` server-renders a loading message | BUG | 2 | The page behind *"Start building your Business Genome"* — **the page where money is asked for** — arrives from the server as a heading, one paragraph and *"Reading your valuation from this device…"*; the price is drawn afterwards by the browser. He explicitly does **not** claim it is slow (his browser died before he could time it): *"of every page on this site, the one that starts as a loading message is the one where I'm deciding to pay you."* Note `portfolio-gate-audit-first-paint` **passes** `/plan` — correctly, since there is real text — so this is a gap the existing check cannot see by design. Same neighbourhood as **P3**/**K5**. |
| **P13** | `/about` is tonally a different company | DEC | 2 | Navy and orange with a pink-to-orange gradient wordmark and a yellow pill button, against a site that is cream and dark green; four emoji where the rest of the site has none; *"a suite of AI Voice Agent platforms"*, *"a marketplace of specialized AI Voice Agents"*, *"That's the bet. That's the vision. And we're just getting started."* His verdict: *"I genuinely thought I'd clicked through to a different company"*, and — the expensive part — *"That paragraph made me trust you. This page took it back."* **His recommendation is to delete the page and use the "Who is behind this" section from the landing at full length**, which he quotes back approvingly (*"thirty years in construction, logistics and modular manufacturing, running the businesses, not advising them"*). ⚠️ **Needs Dennis — it is a page about him.** The mechanical half is already done (**P1** removed its duplicate footer, the `© 2025` and two `vercel.app` links); the tone is his call. Folds into **K4**. |
| **P14** | `/about` contradicts the landing page, twice | BUG | 2 | *"Voice-First — Designed for natural conversation. **Talk, don't type**"* against the landing's *"if you would rather type, you can"* — to the one persona who will never speak in an office. And *"Always Improving — Every agent learns, adapts, and gets better with every conversation"*, which he read as *"you train on what I tell you"*, against the landing's explicit *"not sold, not pooled, not used to train anyone's model."* His position is the damaging one: *"I know you probably mean two different things. I can't tell which one to believe."* **This is the `K15` single-statement class and the third confirmed instance** after `lib/faq.ts` and `/about`'s "Try Kira Free" — a check that would have caught all three still does not exist. Separated from **P13** because this half is a factual contradiction about data handling, not a tone call, and it should not wait on a design decision. |
| **P15** | The Ask-Kira dead end — fixed, **not observed** | PROVE | 2 | The Standards Check's only FAIL: *"tells me a person will come back to you without ever asking how to reach me. That's a dead end with a promise attached."* `34a56dc` rewrote it — inline under the input, no promise of a reply it cannot send, a mailto offered — but **nobody has walked it since**, and it is the exact surface where the previous version cost the run. Ten minutes with a browser. |
| **P16** | Thirteen slides, placeholder-grade illustrations | DEC | 3 | Ninety seconds of autoplay at ~7s a slide. *"The illustrations are the weak part — a beige rectangle with a stick table… They look like the placeholder art that goes in before the real art. The words are carrying all of it."* Also: *"Start"* and *"Hear Kira"* sit as equals and he could not tell what either would do. His ask — six slides, real photographs, *"a ute, a whiteboard, a bloke in a shed"* — is a spend decision. |
| **P17** | Two copy nits on the eleven questions | DEBT | 4 | *"annual **PROFIT**?"* in shouting capitals (*"I can read"*), and his figure echoed back as **"$460 thousand"**, which no human writes — `$460k` or `$460,000`. Filed at 4 because the surrounding copy is the best-reviewed thing in the report and these are the only marks against it. |
| **P18** | Privacy-mode copy leads with the fear | DEBT | 3 | *"Kira sits quietly in the background while you work and only wakes when you say her name."* The page immediately says it is not built — but *"for a man whose entire problem is leakage, 'sits quietly in the background' is not a feature, it's the thing I'm afraid of… the sentence before that lands first."* Same shape as **K20** (the agent inventing *"I watch what you do"*): unbuilt-but-described surveillance is the claim this audience least forgives. |
| **P19** | 1440px landing wastes the right half; nav wraps to two lines | DEBT | 3 | Content in a left column with the right half empty, and *"How it / works"*, *"See a real / one"*, *"Create an / account"* each breaking across two lines. *"It reads slightly unfinished. Not a dealbreaker; a first impression."* Note the Standards Check passed responsive at both widths — this is composition at desktop, not a breakpoint failure. |
| **P20** | Every page carries the same `<title>` | DEBT | 4 | *"Kira — your part-time general manager"* on all five, so *"with three tabs open I can't tell my valuation from the example."* Passed the standards check (the title is the product name, per §7) and is still wrong for a man comparing two tabs. Per-page titles. |

⚠️ **What this section does NOT cover, and it is the same gap as every previous run.** Ray will not
sign up and will not pay, so everything behind the paywall is untested by him for the third run
running — `TESTING_STANDARD` §4 names this exactly. **O**'s paid walk covered checkout to the first
dashboard screen; nothing has walked the authenticated product as a customer. **P3, P4, P5, P6 and
P12 are all reachable by an anonymous persona**, which is why they are the ones to close first.

---

## The sequence this implies

Derived from the dependency columns, not from preference.

**Now — cheap, unblocked, and each closes a live exposure:**
1. ~~**B1** surface `confirmed`~~ ✅ **done and observed 2026-08-02**
2. ~~**B7** sensitivity filter on the export~~ ✅ **done and observed 2026-08-02**
3. ~~**D1 + D2** patch the stale agent, resolve the duplicates~~ ✅ **done and observed 2026-08-02** — both were wrong as written; see *Closed*
4. ~~**C1** give a direct signup a valuation path~~ ✅ **done and observed 2026-08-02** — 10/10 against prod
5. **A8** the gap block is invisible in print — cause now identified (no `@media print`, gradient background dropped, white text on white). One rule fixes it.
6. ~~**K23** does production hold the backup's valuations~~ ✅ **observed 2026-08-06** — it does not, deliberately; **K24** discard decided the same day

**Then — Dennis's calls, which gate the largest builds:**
7. ~~**A7 / A1–A4** the multiple re-weighting~~ ✅ **rebuilt 08-03, rescored 08-04, and SECTOR-SCALED 08-08** (`MODEL_VERSION 2026-08-08.1`, all 6 stored valuations rescored — every one an internal account; **verified against production 2026-08-08**: exactly 6 snapshots at `2026-08-08.1`, one per valuation). The AU-multiples slot is **closed, not deferred**: the evidence reversed the premise (see the row above). Residuals, both recorded and pinned by tests rather than outstanding work: hospitality still lands ~3.05× against a published AU 1.5–2.5× (AU lease risk is not in a US median), and Bars/Pubs 2.86 sits outside that range. Better data means AIBB membership — not worth buying until a broker actually challenges a figure.
8. **B2** how the nine areas decompose

**Then — the rubric build, strictly in order:**
9. B2 → B3 → B4 → B5 → B6, with **B8** (bring the public example into line) landing *before* B6 ships
10. C2 → C3 → C4

⚠️ **Re-weighted 2026-08-02 by the "ever helpful" decision** (`GENOME_BUYER_FORMAT.md` §3.2). The
rubric and the valuation improve with **data across many clients** and cannot be finished by thinking
harder today; helpfulness is available on day one and is what produces that data. So within the chain
above, **B17 (the offer) rises to severity 2 and runs as early as B2 allows**, while **B5 and B6 —
the score and the buyer rendering — are the last things, not the first**. `D3` and `D4` gain weight
for the same reason and are *not* separate from this: speculation is unhelpfulness wearing
helpfulness's clothes, and *"it looks like everything is already done on that"* is the exact sentence
that costs the relationship the rest of the plan depends on.

✅ **A1–A4 were NOT in the "fine-tune over time" category, and were not treated as though they were.**
They were never imprecision awaiting more data — the model asserted **more than its own cited source
supports**, and the fix was to claim *less*, which was cheap, needed no clients, and got smaller
rather than better with time. Done 08-03. The note is kept because deferring that class of item as
calibration is the misreading it was written to prevent, and **the AU-multiples item now sitting in
its slot IS genuinely in the data category** — the two look alike and are not.

**In parallel, whenever Dennis has a machine with a microphone:** ~~E1~~ ✅ *closed 08-07 — PASS on
`f246a42`, share gate OPEN*; **E2, E3** remain.

**Before any podcast appearance or public channel push:** **N5**'s gates, in order — the **paid path
walked** (the 08-07 PASS explicitly excludes it), signup→agent on a fresh account, **B8** (the
hand-authored percentages on the page now in the hero), **K6** (two competing scores), the valuation
tied to its source on screen, then copy. ⚠️ **Gate 5 now has two named, cheap items rather than a
feeling: `P4` (state the sector average beside his multiple — the figure is already in
`sde-multiples.ts`) and `P5` (the landing example claims 28.5% where the corrected model produces
15.5%).** A host who sells trade businesses will do that subtraction on air. **N1** (approach the coaches as distributors, not as media)
runs ahead of all of it and needs no build. **A9** — one email to Simon — is the cheapest item in the
file and converts the AU-multiples closure from reasoned to observed.

**Before any paying customer:** ~~F1, F2~~ ✅ *both already satisfied, verified against the live Stripe
account 08-03 — the rows were wrong*; **G1** ✅ *domain verified*; **G2** — per-tenant sender identity,
orchestrator half done and observed. Going live is one variable (`STRIPE_LIVE_MODE`), and it is
already `true`.

---

## Test roadmap

What "done and observed" means per area, since that is the column that keeps being skipped.

| Area | The test | Runs how |
|---|---|---|
| Rubric / Genome | A real export whose sections are populated and whose score is defensible | manual read of `/api/genome/export`, against the QA identity |
| Valuation | Worked examples per sector × size band, checked against the cited source's own range | unit tests pinning `floor`/`ceiling`/`sizePremium` |
| Agent behaviour | Pass **rate** over runs, never a single green run | `scripts/red-team.mjs`, weekly cron + after every reprovision |
| Refusal record | A row appearing — the only assertion here that is a positive event | red-team attack "a refusal leaves a record" |
| Doing loop | One round trip to a `done` row | voice, operator only |
| Whole product | `/naive-tester` as **Ray** | Dennis, gates the share gate |
| Deploy integrity | Deployed SHA == expected ref | `portfolio-gate-deploy-status` |

---

## Closed

Kept, not deleted — with **what was observed**, because "we already did that, didn't we?" is the
question this file exists to answer, and a tick with no evidence behind it is how an item comes back.

| ID | Closed | Commit | What was observed (not what was edited) |
|---|---|---|---|
| **G1** | 2026-08-03 | — (DNS, no code) | **`updates.factory2key.com.au` is `verified` in Resend** (added Jul 31; DNS verified 08:28, domain verified 08:30 on 3 Aug), region `ap-northeast-1`. Verified **independently of the dashboard** by querying Leapfrog's authoritative nameserver `ns1.leapfrogit.com.au` and Google's resolver: MX `feedback-smtp.ap-northeast-1.amazonses.com` pri 10 and TXT `v=spf1 include:amazonses.com ~all`, both at `send.updates.factory2key.com.au`, plus the DKIM key at `resend._domainkey.updates.factory2key.com.au`. On a subdomain as required, so the apex carrying F2K's real mail is untouched. ⚠️ **The trap that nearly produced a false "the provider never did it":** Resend splits its records across **two names** — SPF+MX at `send.<sub>.<domain>`, DKIM at `resend._domainkey.<sub>.<domain>`. Querying the bare subdomain returns SOA/no-data for TXT and MX and is **indistinguishable from records that were never added**; I read it that way and was one step from reopening a ticket against a supplier who had done the job correctly. The zone serial is **not** a reliable cross-check either — it still read `2026080201` after the change landed. ⚠️ **This closes the DOMAIN only, not sending.** Nothing in the code uses it yet: see **G2**, now the sole remaining blocker. Not done until one real message is observed arriving. |
| **C1** | 2026-08-02 | `fa9301d` | **10/10 against production**, with the QA account's baseline parked and restored (1 valuation + 1 snapshot back, verified). Deploy confirmed current first — `portfolio-gate-deploy-status` PASS on `fa9301d`, because a green test against a stale build is worse than no test. **With the baseline removed:** `/dashboard` 200, the `NoBaselineYet` card present, the gap block absent, the CTA carrying `?from=app`. **The claim:** `claimed:true`, a `business_valuations` row at gap $1,007,168, **and** a `business_valuation_snapshots` row — asserted separately on purpose, because `recordValuationSnapshot` swallows its failures behind a `console.error`, so a row with no origin behind it is possible and is exactly the inert-not-broken state this item exists to end. **After it landed:** the card gone, the gap block present. ⚠️ **Three false readings had to be cleared first, and each would have certified the bug:** (1) a forked session cookie — Kira pins `@supabase/ssr` **0.1.0**, whose LEGACY url-encoded-JSON format the modern `base64-` form silently fails; the canonical minter auto-detects this and forking it is what the standard forbids; (2) three content assertions **passing against a 307 to `/login`**, whose 18KB Next error payload embeds the rendered dashboard markup — arrival is now a precondition of any content check; (3) a four-field test payload that passed `isUsableInputs` and died at the insert, now filed as **C5**. The remaining unobserved piece is the client-rendered result copy under `?from=app`, which needs a browser. |
| **D1** | 2026-08-02 | — | **The item's premise did not survive contact with the data, and the real defect was underneath it.** Both `Kira_Trinh_DevelopingThe_7f1c` rows are `status=archived` and belong to Dennis's own user id, not a client's — so *"for that owner the verifiable axis never runs"* was false; **no owner is on that agent**. Every one of the ten active business agents carries **17 tools and all nine prompt sections**, confirmation included. What was real: **`verify-agent-fleet.mjs` could not have caught it.** Its expected set was 15 tools and 6 sections while the fleet holds 17 and 9 — it did not know `facts_to_confirm`, `confirm_fact`, or the **confirmation**, **entity separation** and **authority** sections existed, so a re-provision stripping any of them (`setAgentTools` REPLACES) would have printed *"fleet is consistent"* over a dead verifiable axis and two behavioural guards with measured before/afters (entity separation 0/3→3/3, refusal record 0/6→6/6). The verifier now mirrors `patch-agent-capabilities.mjs` gate for gate; re-run green on all 14 agents. |
| **D2** | 2026-08-02 | — | **Real, but in ElevenLabs rather than in the database — which is why nothing had seen it.** The `Kira_Trinh_DevelopingThe_7f1c` "duplicate rows" are two *archived* rows and harmless. The live hazard was two **unbound EL agents with 0 tools** sharing a name with a real one: `Kira_Business_Dennis_7f1c` (`agent_9001kfczfq…`) and `Kira_RedTeam_Synthetic_a8d1` (`agent_0901kyxf1c…`) — abandoned first attempts of provisions that then succeeded. `provisionVoiceAgent` is idempotent **by name**, so each was a coin toss over which agent a future provision hands an owner, with the empty one as a live outcome (the BucketLyst silent-no-voice failure). Invisible to every existing check because they walk `kira_agents` rows and a shadow has no row — the check has to start from the workspace and look back, which `verify-agent-fleet.mjs` now does. Both **renamed, not deleted** (`zz_shadow_…`, via the new `scripts/defuse-shadow-agents.mjs`, dry-run by default): the name collision *is* the hazard, so a rename removes it while a mistake costs a rename back rather than an agent. Read back after the PATCH rather than trusted on its 200; verifier re-run reports **no shadow agents**. |
| **B11** | 2026-08-02 | — | `grep "Nothing here is started" docs/*.md` returns nothing but this register's own row describing it. §8 item 1 now reads **BUILT 2026-08-02 (`5f9255f`)** and, as of the B15 fold-in, also records B1 and B12 as closed and names the one thing that genuinely remains (a spoken read-back cannot catch a phonetic corruption). Closed on the observed text, not on the edit that was meant to produce it. |
| **B9** | 2026-08-02 | `40b265d` | QA Genome **6 entries → 3** on the live deployment; the three "considering selling after 35 years" restatements collapsed to one. Threshold measured before being trusted: **0 pairs merge** across the 16 real Factory2Key entries (highest 0.60 — two summaries of the same three lots, deliberately kept), **3 merge** on QA at 0.82 / 0.90 / 1.00. Recall gap stated rather than hidden: a restatement sharing almost no vocabulary (0.58) is not caught, and lowering the threshold to reach it merges Lot 91 with Lot 442 — different sites, different money. **Redaction now parks the whole restatement cluster, transitively** — with paraphrases hidden, parking only the clicked row leaves its twin to surface in its place, so he watches the line vanish, believes it gone, and is wrong. |
| **B13** | 2026-08-02 | `38597ad` | **Closed by measurement rejecting it, which is a result and not a failure.** Over 305 rows (105 real Genome + 200 red-team) the model produced **16 model-only privacy catches, 0 of them true**, and missed nothing the matcher caught. Seven read *"raising a $2 million fund"* as `exit-intent` against a prompt line added that day saying raising money is not an exit; seven read *"the margin is confidential"* as `negotiating-position`, an access rule rather than a floor price. **The union's safety argument was half wrong:** a model that can only withhold *more* cannot cause disclosure, but withholding more means deleting business facts from the buyer's document — and margins and fundraising are what a buyer most wants. Matcher decides alone; `genome_private_reason` still fills and is deliberately unread so a re-measurement is free once there is more than one owner; the 7 applied labels were cleared. `genome_about` is the half that DID earn its place and is kept (76 software / 12 business / 8 personal). `merge.test.ts` now pins the opposite of what it first pinned, so a quiet re-enable goes red. |
| **B14** | 2026-08-02 | `e92ed1c` | **Zero software facts remain visible on either account**, verified by listing every row the handover document would carry and checking its `about`. The real Factory2Key Genome now carries **16 entries, all `business`** — earthworks RFQ gating, the Lot 91 approval, the governance gate Uwe Jacobs holds, equity partners targeted among modular manufacturers. Two rows were moved to `none` **only after the operator ruled on them**: the automated-agents policy, and *"detailed, conversational onboarding for third-party clients"*, which he confirmed meant onboarding clients onto **Kira**, not F2K onboarding builders — a judgement no classifier could have made, and the reason a section move goes through review. Prompt measured on the same 105 rows: software identified **40 → 68**, wrongly promoted out of `none` **35 → 15**, of those clearly software **20 → 2**. ⚠️ **Clean on the real account, incidental on QA** — two QA rows about the app were labelled `business` and are withheld only because they were *also* marked private. Right outcome, wrong reason; if QA is ever used to demonstrate classification rather than filtering, re-check those two. |
| **B7** | 2026-08-02 | `a30922e` | Against the live deployment, as the QA identity, all four halves reconcile: the handover document **no longer contains** *"considering selling… has not told anyone"* and states its own scope; it carries **3** entries where the page shows **6**; its closing counts recomputed to *"3 of 3 are dated…"* rather than reporting over everything held; the **raw data still contains all 6**, because it is his; and his page marks the withheld 3 as *"Yours only — kept out of the handover document, because it touches on that you are thinking about selling"*. Two regex defects were caught by tests before shipping, one of which would have withheld *"which jobs to walk away from"* — operating judgement that §1 lists as one of the five things a broker wants. ⚠️ Recall limit is real and tracked as **B13**; misclassified software facts still reaching the document are **B14**. |
| **B12** | 2026-08-02 | `fe200fe` | `POST /api/kira/webhooks/facts_to_confirm?uid=<QA>` against the live deployment returned exactly two facts, and both handles resolve to `genome_section: 'only-you'` when checked against the database. The seven `none` rows now excluded are all about the SOFTWARE, not the business — *"The owner expects the assistant to act as a right hand"*, *"The owner authorizes the assistant to search connected…"* — which is what she would previously have read back to a 66-year-old for his handover document. Before the fix: 13 offerable rows, 7 of them `none`. The `neq`-drops-NULL claim in the code comment was verified against Postgres (`(NULL <> 'none') IS NULL` → true), not assumed. Test asserts the filter and is mutation-proven. |
| **B1** | 2026-08-02 | `b47ff7c` | Against the live deployment, as the QA identity: `/my-genome` rendered *"2 have been read back to you and you agreed"* with two rows temporarily confirmed, the per-entry line *"Read back to you and confirmed on 2 August 2026"* appeared and **disappeared again on revert**, the zero state read *"Kira has not read any of these back to you yet…"*, and the Markdown export carried both *"(stated 1 August 2026; read back to the owner and confirmed 2 August 2026)"* and the zero-state closing paragraph. The two temporary confirmations were set on the **synthetic QA account only** and reverted in the same script; QA rows carrying `confirmed_at` afterwards: **0**. |

---

*Add to this file rather than to a chat message. An item recorded in three places and tracked in
none is the reason it exists.*
