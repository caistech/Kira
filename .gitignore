# Dependencies
/node_modules
/.pnp
.pnp.js
data/beta-testers.json
# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# `vercel env pull` output — a plaintext dump of production credentials on disk.
# Regenerate on demand (`vercel env pull .vercel-env-check`); never commit.
.vercel-env-check

# TypeScript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*.local

.env.local.copy
tool.json
tool2.json
.env*

# Supabase CLI local state (contains pooler/connection info)
supabase/.temp/

# gstack browser daemon state (contains session tokens)
.gstack/

# Naive-tester screenshots — the written reports are committed, the images are not
# (20MB+ per sweep; regenerate by re-running /naive-tester)
naive-tester-reports/**/*.png
naive-tester-reports/**/*.jpg
naive-tester-reports/**/screenshots/

# Collaborator commercial terms — named individuals, proposed splits, unsigned.
# Deliberately local: a private repo is still the wrong place for someone else's
# unagreed compensation, and it only takes one `git add -A` to publish it to
# everyone with clone access.
docs/collaborator-terms/

# Git worktrees another session creates while working on a branch. This is a FULL SECOND CHECKOUT
# of this repo — committing it would nest a copy of the repo inside itself. vitest already excludes
# it from test discovery for the same reason.
.claude/worktrees/

# Fetched Genome artefacts. These are exports of an account's own record — even from a
# synthetic QA account they are the shape of a thing that must never be committed
# (PRODUCT_STANDARDS: data exports are treated as PII).
manual-*.html

# Local debug/secret artifacts — never commit test keys, secret test scripts, or
# scratch temp files (SECURITY: these contain or read local credentials).
test_key.txt
phase0_test_key*.txt
test_sb_secret.mjs
create_test_users.sql
.tmp-*.log
.tmp-*.mjs
.tmp-dev*

# Scratch / debug outputs & diagnostics — local investigation only.
*.out.txt
policy_counts.txt
mgmt_anon_clean.txt
fks.txt
push_out*.txt
signing-keys-doc.html
phase0_*_test.mjs
phase0_2min_test.cjs
extract-diag.mts
genome-diag.mts
scripts/test-auth-context.ts
create_and_test.ps1
deploy_preview.ps1
rls_policies.txt
tables.txt
test-output*.txt
users_fks.txt
docs/powershell searches.txt

# Audit-blocked files (live credentials / real PII) — excluded until redacted.
scripts/record-beta-evidence.mjs
scripts/find-user-agents.mjs
scripts/create-missing-agent.mjs
scripts/fix-agent-voice.mjs
scripts/reprovision-agent-full.mjs
scripts/reprovision-agent-full.mts
docs/SUPABASE_API_KEY_MODEL_MIGRATION_VALIDATION.md
docs/KIRA*P0.2-A.1*
P2.4-E_PLAN.md
supabase/migrations/20260829150000_p24e10_identity_bootstrap.sql
supabase/migrations/archive/

# Session-local project state (PROJECT_STATE / INCIDENT capture) - per-project save-game,
# not part of the committed codebase.
.context/
