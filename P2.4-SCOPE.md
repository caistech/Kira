# P2.4-SCOPE — Strangler Fig Execution Scope

**Authority:** P2.4-E Plan (Bridge Safety & Retirement), P2.3 Resource Ownership Audit
**Status:** IN PROGRESS (Session: 2026-09-15)

---

## 0. High-Level Goal
Rebind all remaining legacy `user_id` ownership paths to the canonical Organisational Model and retire the legacy `users` table as an identity authority.

---

## 1. Phase 0 — Pre-gate (Identity & Data Repair)
**Blocker (E1.0):** 15 live auth users have zero `auth_credentials`; 6 legacy `users` have no memberships. Canonical chain must be populated before fallback removal.

- [ ] **E1.0-A Identity Bootstrap:** Create `persons` + `auth_credentials` for all 15 production auth users.
- [ ] **E1.0-B Membership Evidence:** Establish `organisation_memberships` for the 6 production users based on definitive evidence (`subscriptions` -> `introductions`).
- [ ] **E2 Data Repair:** 
    - Decision: Assign 41 orphan `genome_facts` rows to an `UNAFFILIATED` holding org (Option A/B in P2.4-E §3).
    - Decision: Assign 38 sentinel rows to `SYSTEM` org.

---

## 2. Phase 1 — E3 Application Rebind (The ~11 Live Refs)
Migrate remaining ownership-keyed reads/writes from `user_id` to `organisation_id` with `person_id` provenance.

- [ ] **Memory Cluster:** `lib/kira/convai.ts`, `uid-tools.ts` (save_memory), `refusal.ts`, `app/api/kira/create/route.ts`.
- [ ] **Genome Cluster:** `lib/genome/access-log.ts`, `app/api/genome/manual/route.ts` (fix drafts-table runtime defect).
- [ ] **Swarm Cluster:** `lib/kira/swarm/open-tasks.ts`, `stub.ts`.
- [ ] **PubGuard/Voice:** `app/api/pubguard/scan/save/route.ts`, `lib/voice-agent-checks.ts`.
- [ ] **Admin Tooling:** `lib/admin/exec.ts`, `exec-reprovision.ts`, and Admin Panel UI.

---

## 3. Phase 2 — Billing & Introducer
- [ ] **Billing Migration:** Move subscription state off legacy `users` table to canonical `subscriptions.organisation_id` in `app/api/billing/*`.
- [ ] **Introducer:** Board/Admin reads moved from `user_id` joins to `organisation_memberships`.

---

## 4. Phase 3 — E4 Users Retirement
- [ ] **Redefine `users`:** Redefine legacy `users` as a projection for billing/notifications only, keyed to `person_id`.
- [ ] **Retire Authority:** Remove all direct-fallback branches in `resolve_organisational_context()` / `resolve_organisation_id_from_auth()` / `link_or_create_app_user()`.

---

## 5. Phase 4 — E5 Final Verification
- [ ] **Integrity Audit:** 
    - Zero NULL-organisation rows on knowledge/memory/tasks.
    - Zero `users.id`-as-org-anchor patterns in app/SQL.
    - Fix `verify_legacy_authority_retired()` definitional false-positive.

---

## Execution Rules
1. **Surgical Rebind:** No mechanical string replacement; resolve Person -> Organisation via canonical chain at every boundary.
2. **Strangler Fig Contract:** Old paths work until new paths are verified; no destructive deletions until E5.
3. **Verification:** `npx tsc --noEmit` + build + vitest must remain green after every phase.
